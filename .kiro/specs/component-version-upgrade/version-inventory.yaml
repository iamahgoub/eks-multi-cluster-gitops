# Version Inventory — component-version-upgrade
#
# The recorded output of the Version_Scan. Consumed directly by the
# Verification_Process for the duplicated-reference consistency check
# (verification check 9, Requirement 10 criterion 13) and the documentation
# drift check (verification check 11, Requirement 9 criterion 17). No rendered
# markdown view of this file is maintained, so that versions have exactly one
# home.
#
# ---------------------------------------------------------------------------
# STRUCTURE
# ---------------------------------------------------------------------------
#
# A mapping with a single `entries` key holding a sequence. Each entry is one
# Version_Reference: one occurrence of one version string in one file.
#
# ONE ENTRY PER OCCURRENCE. `file` is a single repository-relative path and is
# never a list. A Pinned_Component whose version is stated in five files
# produces five entries, not one entry with five paths. This is what makes
# Requirement 1 criterion 2 decidable — entry count equals occurrence count —
# and it is what lets verification check 9 report a divergent occurrence by its
# own path.
#
# ---------------------------------------------------------------------------
# FIELDS
# ---------------------------------------------------------------------------
#
# Required on every entry:
#
#   component   Pinned_Component name, e.g. `flux-source-controller`
#   category    One of: k8s-version, helm-chart, container-image,
#               crossplane-package, k8s-api-version, cli-tool, python-dist,
#               npm-package, aws-resource
#   file        Repository-relative path — a single path, never a list
#   locator     YAML field path, environment variable name, or line number
#               containing the version string
#   current     Version string exactly as it appears in the file
#   target      Target_Version
#   status      Exactly one of the six status flags below
#
# Conditional:
#
#   successor        Required when `status` is `superseded` or `removed`.
#                    Names the replacing artifact or component.
#   source_url       URL of the source of record. Required unless `status` is
#                    `no-source`.
#   consulted        ISO-8601 date the source was consulted. Required whenever
#                    `source_url` is present.
#   duplicate_group  Shared identifier carried by every entry of a
#                    Duplicated_Reference. Absent on a single-occurrence
#                    reference.
#
# Optional:
#
#   notes            Migration mechanics, e.g. a field rename forced by the
#                    target.
#
# ---------------------------------------------------------------------------
# STATUS FLAGS — exactly one applies per entry
# ---------------------------------------------------------------------------
#
#   upgrade       Ordinary bump. `current` resolves upstream and a newer
#                 release exists. (Requirement 1.3)
#
#   unresolvable  `current` is absent from the upstream registry. `current` is
#                 retained verbatim and `target` records the highest GA
#                 non-pre-release version the registry publishes.
#                 (Requirement 1.4)
#
#   superseded    The artifact is replaced by a differently named upstream
#                 artifact. `successor` and `source_url` name it.
#                 (Requirement 1.5)
#
#   no-source     No maintainer page or registry states a version for the
#                 component. `target` equals `current` and `source_url` is
#                 omitted. (Requirement 1.8)
#
#   mutable       `current` is a floating tag rather than a string identifying
#                 a single immutable artifact. `target` is a string that
#                 identifies one immutable artifact. (Requirement 1.9)
#
#   removed       The component is removed by this feature rather than
#                 upgraded. `successor` names its replacement and `target` is
#                 `n/a`. (Requirement 1.10)
#
# ---------------------------------------------------------------------------
# DUPLICATED REFERENCES
# ---------------------------------------------------------------------------
#
# A Duplicated_Reference produces one entry per file path, all sharing a
# `duplicate_group` value (Requirement 1 criterion 6). Every entry in a group
# carries the identical `target` string (Requirement 1 criterion 7).
# Verification check 9 asserts both directions: group membership implies target
# equality, and the value at each stated file path matches that target.
#
# ---------------------------------------------------------------------------
# STATE OF THIS FILE — enumeration and audit complete (tasks 1.2, 1.3)
# ---------------------------------------------------------------------------
#
# Every entry below is a Version_Reference verified present in the
# GitOps_Solution at the stated path and locator. No entry is
# illustrative-only, because Requirement 1 criterion 2 forbids an entry that
# does not correspond to an occurrence.
#
# Task 1.3 closed the two deferred pins, audited every `status` flag and
# every conditional-field rule, and completed `duplicate_group` assignment.
# Zero `<unresolved — task 1.3: ...>` strings remain. The audit state, as
# machine-checked over the 335 entries:
#
#   * status distribution — upgrade 280, removed 40, no-source 6, mutable 5,
#     unresolvable 4. Exactly one flag per entry, all six drawn from the
#     fixed set; no seventh flag was introduced (see FINDING 15). (The
#     openid-connect-provider thumbprint entry moved from `removed` to
#     `upgrade` when DERIVATION 2 was superseded by the documented
#     well-known root-CA constant; target now equals current — see that
#     entry's notes.)
#   * conditional fields — `successor` present exactly on the `superseded`
#     and `removed` entries, `source_url` present on every entry except the
#     six `no-source` ones, `consulted` present on every entry carrying a
#     `source_url`, and all seven required fields non-empty on all 335
#     entries (Requirement 1 criterion 3).
#   * duplicate groups — 65 groups over 295 entries; 40 entries are
#     single-occurrence and correctly carry no group. No group has one
#     member, and every group states one identical `target` (Property 8).
#   * Property 12 — no resolved `target` contains a range operator or a
#     wildcard. 41 targets remain `<unresolved — task N: ...>` placeholders,
#     each owned by a later task: 6.2 ×9, 11.2 ×7, 8.4 ×5, 11.1 ×4, 12.1 ×3,
#     4.3 ×2, 6.1/12.1 ×2, 10.3/10.4 ×2, 11.3 ×2, 8.3 ×2, 6.1 ×1, 8.1 ×1,
#     10.4 ×1. Eight `successor` values carry a task 3.1 placeholder. The
#     only targets that will legitimately BE ranges are the six
#     `npm-package` entries, which Requirement 7 criterion 7 states as
#     ranges and which verification check 7 resolves to an exact published
#     version; every other pin resolves as an exact string.
#
# ---------------------------------------------------------------------------
# SCAN SCOPE — what is enumerated, and what is deliberately not
# ---------------------------------------------------------------------------
#
# Requirement 1 criterion 2 is bidirectional, so the scope boundary has to be
# written down for the criterion to be decidable. Enumerated:
#
#   * Helm chart versions, container image references carrying a tag,
#     Crossplane package references, Kubernetes cluster versions, CLI tool
#     versions in scripts and documentation, Python distribution pins, npm
#     package pins.
#   * API group-versions of the third-party controllers this solution pins:
#     Flux (`*.toolkit.fluxcd.io`), Crossplane (`*.crossplane.io`), Karpenter
#     (`karpenter.sh`, `karpenter.k8s.aws`), External Secrets
#     (`external-secrets.io`), Sealed Secrets (`bitnami.com`), kustomize
#     (`kustomize.config.k8s.io`), eksctl (`eksctl.io`), and the solution's own
#     composite API (`gitops.k8s.aws`, recorded `no-source`).
#   * Group-versions stated OUTSIDE a resource's own `apiVersion`. Three
#     distinct occurrence sites exist in this repository, not the two the
#     design names — see FINDING 3 below.
#
# NOT enumerated, each for a stated reason:
#
#   * Core Kubernetes GA group-versions — `v1`, `apps/v1`, `batch/v1`,
#     `rbac.authorization.k8s.io/v1`, `networking.k8s.io/v1`,
#     `apiextensions.k8s.io/v1`. These are APIs of the cluster itself; their
#     availability tracks Target_Kubernetes_Version rather than any pinned
#     third-party artifact, nothing in this upgrade changes them, and no
#     Correctness Property quantifies over them.
#   * Fixed schema/format identifiers that have no upstream release stream:
#     `AWSTemplateFormatVersion: "2010-09-09"`, IAM policy
#     `Version: 2012-10-17` (7 occurrences), SSM document
#     `schemaVersion: "2.2"` (10 occurrences), the CodeBuild buildspec
#     `version: 0.2` (`cfn.yaml` line 1295), and `lockfileVersion: 2` in
#     `package-lock.json`.
#   * `repos/apps/product-catalog-fe/package-lock.json` resolved dependency
#     versions (272 `resolved` entries). The lockfile is a generated artifact
#     regenerated wholesale in task 11.2 from the ranges in `package.json`,
#     which ARE enumerated. Recording 272 derived pins would create 272
#     entries that no edit targets.
#   * Image references that state a repository but no tag —
#     `kubecost-release.yaml` `kubecostFrontend.image`, `kubecostModel.image`,
#     `networkCosts.image.repository`, `clusterController.image.repository`,
#     `prometheus.server.image.repository`,
#     `prometheus.configmapReload.prometheus.image.repository`. A
#     Version_Reference is an occurrence of a *version string*; these carry
#     none, taking their tag from the chart. They are listed here so their
#     absence is deliberate rather than an omission.
#   * `pod-security.kubernetes.io/warn-version: latest` in
#     `clusters-config/template/secrets/namespace.yaml` — a Pod Security
#     Admission setting whose literal value is `latest`, not an artifact tag.
#   * `latest` inside URLs that are not artifact selectors: IMDS paths
#     (`169.254.169.254/latest/...`, 3 occurrences) and AWS documentation
#     links (`docs.aws.amazon.com/.../latest/...`).
#   * Unpinned installers that state no version string at all: the AWS CLI v2
#     installer in `initial-setup/README.md` (line 79), and the `kubeseal` and
#     `yq` "resolve the latest release tag from a redirect" blocks in
#     `initial-setup/auto/cfn.yaml` (lines 284-288 and 299-303). These pin
#     nothing and contain no version string, so they are not
#     Version_References — but they are the reason `cfn.yaml` and
#     `initial-setup/README.md` disagree about `kubeseal` and `yq`, which
#     task 12.1 has to reconcile.
#   * Example version numbers in walkthrough prose that name the sample's own
#     application git tags (`scenarios.md` `v2.0`, 5 occurrences). These are
#     tutorial values for the reader's own tags, not third-party pins.
#   * `initial-setup/auto/reference/code-editor.yaml`. It is an untracked
#     reference template that task 10.3 copies *resource patterns* from; it is
#     not itself deployed by the GitOps_Solution and is not listed among the
#     task 1.2 surfaces. If the finished tree ends up carrying it as a
#     tracked file, its pins need a follow-up scan.
#
# ONE DOCUMENTED GRANULARITY EXCEPTION. The distribution version label
# `app.kubernetes.io/version: v2.1.2` appears 35 times in each
# `gotk-components.yaml` — once on every object in a wholly generated file.
# It is recorded as one entry per file rather than 35, with the locator naming
# the label key and the occurrence count. Everything else, including repeated
# group-versions within a single file, is enumerated per occurrence. The
# rationale: `file` stays a single path (which is the constraint that matters
# for check 9), 70 byte-identical entries would add no verification power, and
# check 9 can still decide the criterion by asserting that *every* occurrence
# of that label in that file equals the target. Flagged here so task 1.3 can
# overrule it.
#
# ---------------------------------------------------------------------------
# FINDINGS — where the repository contradicts the requirements or the design
# ---------------------------------------------------------------------------
#
# FINDING 1 (confirmed). The Flux CLI is NOT a Duplicated_Reference pair
# stating `0.35.0` in two files. `initial-setup/auto/cfn.yaml` line 266 pins it
# (`FLUX_VERSION=0.35.0`); `initial-setup/README.md` line 145 is
# `curl -s https://fluxcd.io/install.sh | sudo bash` — entirely UNPINNED, with
# no version string present. Design area B, task 1.3 and task 12.1 all
# describe `0.35.0` in both files. The README entry below records what is
# actually there (`status: mutable`, `current` quoting the unpinned command)
# rather than a `current` value that does not exist. Both entries carry
# `duplicate_group: flux-cli-version` with one identical target, so check 9
# still catches divergence once task 12.1 pins the README.
#
# FINDING 2 (confirmed). `initial-setup/auto/cfn.yaml` has NO `Mappings:`
# section. Its top-level keys are `AWSTemplateFormatVersion`, `Description`,
# `Parameters`, `Resources`, `Outputs`. The Kubernetes-version → kubectl-patch
# selection is a bash `case` statement inside the `InstallK8sClientToolsDoc`
# SSM document (lines 178-190). Design area A, task 6.2 and verification
# check 12 all describe rewriting a "kubectl download `Mappings`" block, and
# Correctness Property 9 asserts key-set equality between `AllowedValues` and
# that `Mappings` block. Check 12 as written has nothing to parse. It needs to
# be restated against the `case` branches.
#
# FINDING 3 (confirmed). There are THREE kinds of Kubernetes-version list in
# `cfn.yaml`, not one, and the design names only the first:
#   a. `Parameters.KubernetesVersion.AllowedValues` (lines 18-20)
#   b. `InstallK8sClientToolsDoc` `parameters.Version.allowedValues`
#      (lines 131-133)
#   c. `CreateEKSClusterDoc` `parameters.Version.allowedValues`
#      (lines 375-377)
# All three currently state `1.29`/`1.28`/`1.27`. All three must be rewritten
# together or SSM rejects the value that CloudFormation accepted. Property 9
# should quantify over all three plus the `case` branches.
#
# FINDING 4 (confirmed). `ConfigureWorkshopEnvironmentDoc`
# `parameters.Version.default` is `"1.22"` (`cfn.yaml` line 888) — a fourth,
# stale Kubernetes version that appears in no `AllowedValues` list and in no
# requirements or design table.
#
# FINDING 5 (confirmed). `bin/*.sh` contains NO Flux or Crossplane
# group-version string. The architecture note "Generators follow manifests",
# design area B mechanic 2 ("the heredocs and `sed`/`yq` templates inside
# `bin/*.sh` that generate manifests"), Requirement 8 criterion 12, task 3.1
# surface 4, and the "shell scripts included" clause of Properties 1 and 2 all
# rest on that claim. What the scripts actually do: `add-cluster.sh`,
# `add-cluster-app.sh` copy template directories and rewrite *placeholder
# names* with `sed`; `add-cluster-app.sh`, `remove-cluster*.sh`,
# `update-cluster-app.sh` use `yq` with *field paths* only. The single heredoc
# that emits a manifest is `bin/add-app-cluster-overlay.sh` line 15, and it
# emits `apiVersion: kustomize.config.k8s.io/v1beta1` — a kustomize config API
# version, not a Flux one. It is recorded below. Task 3.1 surface 4 has no
# work to do beyond confirming this.
#
# FINDING 6 (confirmed). `repos/gitops-system/tools-config/external-secrets/
# sealed-secrets-key.yaml` declares `SecretStore` and `ExternalSecret` on
# `external-secrets.io/v1alpha1` (lines 14 and 29). External Secrets Operator
# is being taken from chart `0.4.4` to `2.10.0` — two major lines — and
# `v1alpha1` is not served by current ESO. Neither the requirements' inventory
# table nor design area D mentions these two group-versions; Requirement 5 and
# Property 6 cover only values-schema keys. This is a required API migration
# inside task 8.3 that is currently unscoped.
#
# FINDING 7 (confirmed). Group-versions stated outside a resource's own
# `apiVersion` occur at a THIRD site the design does not name: kustomize
# `spec.patches[].target` `group` + `version` pairs. Design area B mechanic 3
# and task 1.2/3.1 list `spec.dependsOn`, `spec.healthChecks[].apiVersion`,
# `spec.sourceRef` and `spec.chart.spec.sourceRef`. In this repository:
#   * `spec.dependsOn` entries carry only `- name:` — no group-version at all.
#   * `spec.sourceRef` and `spec.chart.spec.sourceRef` carry only `kind`,
#     `name`, `namespace` — no group-version at all.
#   * `spec.healthChecks[].apiVersion` does carry them (enumerated below).
#   * `spec.patches[].target.group` + `.version` carries 22 of them,
#     including two Flux ones (`kustomize.toolkit.fluxcd.io` + `v1beta2` in
#     `clusters/mgmt/crossplane.yaml` and `clusters/template/crossplane.yaml`)
#     and one Crossplane one (`pkg.crossplane.io` + `v1alpha1` in
#     `clusters/template/crossplane.yaml`, nested two levels deep inside a
#     patch body).
#   * Inline strategic-merge patch bodies are YAML-in-a-string and carry a
#     further 2 Karpenter `apiVersion` occurrences
#     (`clusters/{mgmt,template}/karpenter-config.yaml` line 27).
# A `patches[].target` may also state `group` with no `version` (5
# occurrences: `helm.toolkit.fluxcd.io` ×3, `karpenter.k8s.aws` ×2); those are
# not Version_References and are not enumerated.
#
# FINDING 8 (confirmed). `InstallK8sClientToolsDoc` hard-fails on any Amazon
# Linux other than 2: `if [[ "$OS_VER" != "2" ]]; then ... exit 103`
# (`cfn.yaml` lines 160-164). Requirement 2 criterion 20 and design area G
# put the Dev_Environment on AL2023, and Requirement 2 criterion 17 retains
# and retargets this document. As written the retargeted document will exit
# 103 on the new instance. Task 10.4 needs this guard updated.
#
# FINDING 9 (confirmed). Two container/runtime pins in `cfn.yaml` appear in no
# requirements or design table: the CodeBuild image
# `aws/codebuild/amazonlinux2-x86_64-standard:3.0` (line 1262) and the Lambda
# `Runtime: python3.8` (line 1463). `python3.8` is past Lambda's deprecation
# for new function creation, so the stack cannot be created as written even
# after Cloud9 is removed. Task 10.3/10.4 territory. Both are enumerated
# below.
#
# FINDING 10 (confirmed). Two CLI installs resolve to "whatever is latest at
# run time" and are therefore unpinned: `eksctl` in
# `initial-setup/README.md` line 165 and in `cfn.yaml` line 248, both via
# `/releases/latest/download/`. Requirement 2 criterion 10 needs an eksctl
# Target_Version and design area A names `eksctl v0.230.0`, but no
# requirements table records where eksctl is pinned. Both occurrences contain
# the literal string `latest` and are enumerated as `mutable`.
#
# FINDING 11 (confirmed). `README.md` line 81 links
# `https://external-secrets.io/v0.5.7/provider-aws-secrets-manager/` — a
# version-pinned documentation URL for external-secrets that no table
# records. It has to move with the chart bump or Requirement 9 criterion 17
# (documentation drift) fails on it.
#
# FINDING 12 (confirmed). `scenarios.md` states the cluster upgrade scenario
# as `1.23` → `1.24` (lines 342 and 362, four version occurrences), not
# `1.28` → `1.29`. The documentation was already drifted from
# `eks-cluster.yaml` before this upgrade. Design area I requires it to read
# `1.35` → `1.36`.
#
# FINDING 13 (schema gap, for task 1.3). The six status flags have no value
# for "already current" — `target` equals `current`, a source of record
# exists, and no edit is needed. `upgrade` is defined as "a newer release
# exists" (false) and `no-source` requires `source_url` to be omitted (also
# false). This applies to a large class: every retained Crossplane
# managed-resource group-version (design area C: "unchanged, subject to
# per-group confirmation"), `kustomize.config.k8s.io/v1beta1`,
# `bitnami.com/v1alpha1`, and the sample's own image tags. Those entries carry
# `status: upgrade` below as a best-effort placeholder with `target` equal to
# `current`. Task 1.3 owns the flag audit and should either add a seventh flag
# or state which existing flag covers this.
#
# FINDING 14 (correction to the seed set). The task 1.1 header stated that no
# `no-source` instance was confirmed in the repository. The scan found some:
# the `gitops.k8s.aws/v1beta1` composite API version, which this repository
# defines itself in `compositeresourcedefinition.yaml` and for which no
# upstream registry states a version. Those entries below carry
# `status: no-source` with `target` equal to `current` and no `source_url`,
# per Requirement 1 criterion 8. No `superseded` instance was found —
# consistent with the requirements' correction that
# `crossplane-contrib/provider-aws` is NOT superseded.
#
# FINDING 15 (resolves FINDING 13; task 1.3). No seventh flag is added, and
# none is needed: `upgrade` is the RESIDUAL flag, and it covers the
# "already current" class. The reasoning, since FINDING 13 read the flag set
# as incomplete:
#   * Requirement 1 defines five flags by IF/WHERE conditions —
#     `unresolvable` (1.4), `superseded` (1.5), `no-source` (1.8), `mutable`
#     (1.9), `removed` (1.10). Each fires only when its condition holds.
#     `upgrade` is backed by criterion 1.3, which states field obligations
#     only and says nothing about a newer release existing. So at the
#     requirements level `upgrade` means exactly "none of the five
#     conditions fires", which is true of an entry whose `target` equals its
#     `current`.
#   * The "a newer release exists" clause is design prose glossing the
#     common case, not requirement text. It is narrowed here rather than
#     treated as a definition; the flag set stays fixed at six, as
#     Requirement 1 requires.
#   * `no-source` was considered and rejected for this class. It is
#     factually false — a source of record DOES exist for these entries and
#     is recorded (for the retained Crossplane managed-resource
#     group-versions it is the CRD set shipped in the pinned provider
#     package, which task 4.1 must confirm per group-version) — and
#     adopting it would force `source_url` to be omitted on 144 entries,
#     discarding the confirmation URL and leaving Requirement 1 criterion 3
#     unsatisfied for them.
#   * Neither verification check 9 nor check 11 is affected: both compare
#     against `target`, which is non-empty either way. The class needs no
#     marker field because it is mechanically identifiable — `target`
#     equals `current`, and no edit is required at that occurrence. 150
#     entries are in that state: 144 `upgrade` and the 6 `no-source`.
#
# FINDING 16 (task 1.3, corrects the design; re-pinned to 2.8.4 in the live
# run). There is NO `8.15.x` line of the kubecost cost-analyzer chart. Design
# area D and task 8.4 both name one. The chart's own version stream is 1.x
# then 2.x; the string `8.15.X` in
# https://kubecost.github.io/cost-analyzer/index.yaml is the dependency
# version constraint on the bundled `prometheus` SUBCHART of the 1.x
# cost-analyzer charts. Task 8.4 should stop describing an `8.15.x` line.
#   The target was first recorded as 2.9.6 (highest cost-analyzer tag then
# resolvable through this repository's `kubecost-repository`,
# `oci://public.ecr.aws/kubecost`). The live end-to-end run showed 2.9.x is
# UNUSABLE as a standalone fresh-install target: the entire 2.9.x line is a
# "prepare to upgrade to 3.0" TRANSITION release that hard-requires federation
# / transition config — cluster_id in two places (FINDING 24b) plus a global
# federated store — rather than being a normal GA install. Resolution: re-pin
# to 2.8.4, the highest STABLE non-transition cost-analyzer in the OCI
# registry `oci://public.ecr.aws/kubecost` (2.8.x tops out at 2.8.4).
# `global.clusterId`/`global.clusterName` (FINDING 24) remain set — they are
# valid and harmless in 2.8.x — but the 2.9-only second cluster_id place and
# federated-store config are dropped (FINDING 24b superseded). Verification
# check 3 resolves a pinned chart against the index of the HelmRepository its
# HelmRelease references, so the recorded target is 2.8.4 — an exact string,
# no wildcard.
#
# FINDING 17 (task 1.3, semantic narrowing task 5.1 inherits). The AL2023
# accelerated AMI type is `AL2023_x86_64_NVIDIA`, verified published for
# BOTH cluster tiers — the recommended-AMI SSM parameters
# /aws/service/eks/optimized-ami/{1.36,1.35}/amazon-linux-2023/x86_64/nvidia/recommended/image_id
# both resolve (us-west-2, 2026-09-15) — and the string is drawn from the
# `Nodegroup` `amiType` enum in the EKS API reference, which is the form
# task 5.1 must write. The narrowing: `AL2_x86_64_GPU` served NVIDIA GPU
# and AWS Neuron instances from ONE AMI, whereas AL2023 splits them into
# `AL2023_x86_64_NVIDIA` and `AL2023_x86_64_NEURON`. The composition's map
# transform has exactly two branches keyed on `workload-type`
# (`non-gpu`/`gpu`), so after task 5.1 the `gpu` branch covers NVIDIA only.
# A reader who was running Inferentia or Trainium instances through the
# `gpu` branch loses that path and needs a third parameter value. Both
# AL2023 accelerated variants are published for 1.36 and 1.35, so adding a
# branch is possible; it is outside this task and is not currently scoped by
# any requirement.
#
# FINDING 18 (task 1.3, corrects a duplicate group). The Sealed Secrets
# "version triple" the design and task 1.3 describe — chart pin, namespace
# label, `kubeseal` install command — is NOT a valid duplicate group, and
# the group recorded below is a different set. All three members were
# verified present: chart `2.7.1` at
# tools/sealed-secrets/sealed-secrets-release.yaml line 15, label `v0.26.3`
# at clusters-config/template/secrets/namespace.yaml line 7, and the
# `kubeseal` install at initial-setup/README.md lines 150-151. Two problems:
#   a. THE CHART PIN IS A DIFFERENT VERSION STREAM. The chart version
#      (2.7.1 → 2.20.0) and the controller version it deploys (v0.26.3 → a
#      0.x release) are unrelated numbering. Requirement 1 criterion 7 and
#      Property 8 require ONE identical target per group, which a group
#      spanning both streams can never satisfy. The chart pin is therefore
#      excluded from the group; Requirement 5 criterion 11 is what couples
#      it to the label, and task 8.4 plus verification check 3 enforce that,
#      not check 9.
#   b. THE REMAINING FOUR OCCURRENCES ARE ONE STREAM IN TWO NOTATIONS. The
#      group is the namespace label plus THREE `kubeseal` occurrences (the
#      release-tag path segment and the tarball file name on line 150, and
#      the tarball file name on line 151) — four, not three. The label and
#      the release-tag segment write the version `v`-prefixed; the two
#      tarball names write it bare. Property 8's second clause — every file
#      occurrence states the group's target string — is then false by
#      NOTATION, not by drift, so check 9 as specified would report a
#      permanent false failure. Treatment: the group `target` is recorded in
#      canonical bare form and each entry's `notes` declares its notation,
#      so check 9 strips one leading `v` at the two `v`-prefixed
#      occurrences. This is the only group in the file with mixed notation.
#      Note also that the group asserts slightly more than the requirements
#      do — criterion 11 constrains the label to the controller's full
#      major.minor.patch while criterion 12 constrains `kubeseal` only to
#      major.minor — which is satisfiable because both artifacts ship from
#      one upstream sealed-secrets release tag.
#
# FINDING 19 (task 1.3, completes duplicate-group assignment). Task 1.2 left
# 44 groups covering 94 entries; 201 further entries were multi-occurrence
# but ungrouped, including every repeated API group-version. Those are
# Duplicated_References under the glossary — the same Pinned_Component, for
# the same purpose, in at least one other file — so criteria 6 and 7 apply
# and 21 groups were added, the largest being
# `crossplane-iam-group-version` (55 occurrences over 18 files),
# `kustomize-config-api-version` (37 over 37) and
# `flux-kustomization-group-version` (36 over 34). Two rules were needed to
# make the assignment decidable, and both are recorded here rather than
# inferred:
#   a. SCOPE. A group is assigned to every set of two or more occurrences of
#      one Pinned_Component that must state one identical string, whether or
#      not the occurrences span files. Three groups are single-file sets
#      (`crossplane-ec2-group-version` 13 occurrences in composition.yaml,
#      `kubectl-release-date-cfn` 3 in cfn.yaml,
#      `external-secrets-group-version` 2 in sealed-secrets-key.yaml). They
#      are not Duplicated_References by the glossary's "at least one other
#      file" wording, but grouping them costs nothing and gives check 9 a
#      per-occurrence assertion. The Duplicated_Reference subset is exactly
#      those groups whose members hold two or more distinct `file` values,
#      which keeps criterion 6 decidable.
#   b. SPLITTING BY TARGET, NOT BY NAME. Occurrences of one group-version
#      that resolve to DIFFERENT targets are different sets. The instance is
#      `karpenter.k8s.aws/v1beta1`: three occurrences move to
#      `karpenter.k8s.aws/v1` and form `karpenter-ec2nodeclass-group-version`,
#      while the fourth — the `NodePool.spec.template.spec.nodeClassRef`
#      apiVersion — is a structural change to the v1 `group`/`kind`/`name`
#      form, so it stays ungrouped rather than being forced to share a
#      target it cannot state.
#   Consequence for check 9: 22 of the grouped occurrences are
#   `spec.patches[].target.group` + `.version` pairs (FINDING 7), where the
#   file states the group and the version under two separate keys. Their
#   `current` and `target` are recorded as the COMPOSED `group/version`
#   string, so check 9 must join the two keys with `/` before comparing at
#   those paths.
#
# FINDING 20 (task 1.3, confirms FINDING 1's treatment). The Flux CLI pair
# is sound as task 1.2 recorded it. `cfn.yaml` pins `FLUX_VERSION=0.35.0`;
# `initial-setup/README.md` line 145 is `curl -s https://fluxcd.io/install.sh
# | sudo bash`, still verified unpinned. Keeping `duplicate_group:
# flux-cli-version` on both, with the README entry `status: mutable` and its
# `current` quoting the command, is the right call on three grounds: the
# occurrence is a real edit site that task 12.1 must pin; an installer that
# resolves to whatever is latest at run time is precisely what Requirement 1
# criterion 9 covers, since it names no immutable artifact; and the group is
# what makes check 9 catch the two files disagreeing once the README is
# pinned. Until task 12.1 lands, check 9 correctly reports the README
# occurrence as not stating the target — that is drift being detected, not a
# defect in the entry. The same treatment covers the two `eksctl`
# `/releases/latest/download/` occurrences (FINDING 10).
#
# FINDING 21 (task 1.3, unresolved but recorded). The `consulted` dates on
# the entries task 1.2 wrote are all 2026-11-20, which is two months in the
# FUTURE of the system date on which task 1.3 ran (2026-09-15). Requirement
# 1 criterion 3 requires the calendar date on which the source was
# consulted, so at most one of the two dates can be right. The task 1.3
# entries carry 2026-09-15. The task 1.2 dates are left untouched rather
# than rewritten across ~300 entries on a guess about which clock is wrong;
# whoever knows should correct them in one pass. Nothing downstream reads
# `consulted`, so this blocks no check — but verification check 14, which
# asserts both Kubernetes versions are in standard support "on the day the
# check runs", is the place where a wrong date would be noticed.
#
# FINDING 22 (task 9.1, confirmed — NOT a blocking finding). The EKS cluster
# authentication mode IS settable on the managed resource that creates the
# Workload_Cluster, so Requirement 6 criterion 7 can be satisfied at creation
# and task 9.2 has a field to write. The `Cluster` CRD shipped in
# `crossplane-contrib/provider-aws:v0.59.0` for `eks.aws.crossplane.io/v1beta1`
# exposes an optional access-config object on `spec.forProvider`. The exact
# settable field path task 9.2 must set on the `eks-cluster` resource in
# `repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml`
# is:
#
#     spec.forProvider.accessConfig.authenticationMode
#
# It is a string enum constrained to exactly `API`, `API_AND_CONFIG_MAP`,
# `CONFIG_MAP` (kubebuilder `+kubebuilder:validation:Enum=API;API_AND_CONFIG_MAP;CONFIG_MAP`),
# which is precisely the value set Requirement 6 criterion 7 permits. Task 9.2
# sets `API_AND_CONFIG_MAP` there so the `aws-auth` ConfigMap path
# (`CONFIG_MAP`) keeps working while `API` is also enabled — stated at
# creation, since `API` cannot later be disabled and `CONFIG_MAP` cannot be
# added to a cluster created without it.
#
# Evidence (verified 2026-09-20, no crane/docker available so read from the
# pinned tag's source and generated CRD rather than the package image):
#   * apis/eks/v1beta1/types.go @ v0.59.0 — `ClusterParameters` (the Go struct
#     that generates `spec.forProvider`) carries `AccessConfig *AccessConfig
#     `+"`"+`json:"accessConfig,omitempty"`+"`"+`` as its first field, and the
#     `AccessConfig` struct carries `AuthenticationMode *AuthenticationMode`
#     with json tag `authenticationMode`.
#   * package/crds/eks.aws.crossplane.io_clusters.yaml @ v0.59.0 — the served
#     `v1beta1` schema resolves
#     `.spec.properties.forProvider.properties.accessConfig.properties.authenticationMode`
#     as a string with `enum: [API, API_AND_CONFIG_MAP, CONFIG_MAP]`.
#   Source of record:
#   https://github.com/crossplane-contrib/provider-aws/blob/v0.59.0/package/crds/eks.aws.crossplane.io_clusters.yaml
#   (and the sibling apis/eks/v1beta1/types.go at the same tag).
# The management-cluster path (task 9.2) is separate: it is created by eksctl,
# not this managed resource, and states `API_AND_CONFIG_MAP` in the eksctl
# access-config block of `initial-setup/config/mgmt-cluster-eksctl.yaml`.
#
# FINDING 23 (task 4.1, confirmed — NOT a blocking finding). All three
# `v1alpha1` managed-resource / `ProviderConfig` group-versions this repository
# declares ARE served by the pinned provider packages, so Requirement 4
# criteria 5 and 6 and Property 4 hold and zero managed resources remain on an
# unserved group-version. Read from the pinned tags' generated CRDs (crane and
# docker unavailable, so the package `package/crds/*.yaml` at the pinned tag was
# read directly rather than the package image), verified 2026-09-20:
#
#   1. `eks.aws.crossplane.io/v1alpha1` (`NodeGroup`) — served by
#      `crossplane-contrib/provider-aws:v0.59.0`.
#      package/crds/eks.aws.crossplane.io_nodegroups.yaml @ v0.59.0:
#      `spec.group: eks.aws.crossplane.io`, `versions[0].name: v1alpha1`,
#      `served: true`, `storage: true`. Used at composition.yaml line 544.
#   2. `dynamodb.aws.crossplane.io/v1alpha1` (`Table`) — served by
#      `crossplane-contrib/provider-aws:v0.59.0`.
#      package/crds/dynamodb.aws.crossplane.io_tables.yaml @ v0.59.0:
#      `spec.group: dynamodb.aws.crossplane.io`, `versions[0].name: v1alpha1`,
#      `served: true`, `storage: true`. Used by the sample app table at
#      repos/apps-manifests/product-catalog-api-manifests/v2-staging/kubernetes/overlays/staging/infra.yaml
#      line 1.
#   3. `kubernetes.crossplane.io/v1alpha1` (`Object`, `ProviderConfig`) —
#      served by `crossplane-contrib/provider-kubernetes:v1.3.1`.
#      * package/crds/kubernetes.crossplane.io_providerconfigs.yaml @ v1.3.1:
#        `spec.group: kubernetes.crossplane.io`, `versions[0].name: v1alpha1`,
#        `served: true`, `storage: true`.
#      * package/crds/kubernetes.crossplane.io_objects.yaml @ v1.3.1: the
#        `v1alpha1` version is `served: true` but `storage: false` and carries
#        `deprecated: true` — v1.3.1 adds a `v1alpha2` `Object` that is the
#        storage version and the schema description states v1alpha1.Object is
#        deprecated in favor of v1alpha2.Object. Being SERVED is what
#        Requirement 4 criterion 5 requires, so this is NOT a blocking finding;
#        the repository's five `Object`/`ProviderConfig` occurrences continue
#        to be admitted at v1alpha1. Migrating the `Object` bases to v1alpha2
#        is a non-breaking future cleanup, not in scope for this task, and is
#        recorded here only so the deprecation is not later mistaken for a
#        removal.
#   Source of record for all three:
#   https://github.com/crossplane-contrib/provider-aws/tree/v0.59.0/package/crds
#   https://github.com/crossplane-contrib/provider-kubernetes/tree/v1.3.1/package/crds
#   The `notes` on the affected entries below carry the per-entry confirmation.
#
# FINDING 24 (live end-to-end run, task 8.4 correction). The kubecost
# cost-analyzer 2.9.6 chart FAILS to install as-is: its bundled `finopsagent`
# subchart aborts with "clusterId is required. Please set
# .Values.global.clusterId". This is a values-schema change across the
# 2.2.2 → 2.9.6 upgrade — `global.clusterId` is a new MANDATORY value that
# 2.2.2 did not require. The offline values-schema diff in task 8.4 could not
# catch it because kubecost publishes no `values.schema.json`, so there was no
# machine-readable schema to diff; the requirement only surfaced in a live
# install. Resolution: set `spec.values.global.clusterId` and
# `global.clusterName` to `${CLUSTER_NAME}` in
# `repos/gitops-system/tools/kubecost/kubecost-release.yaml`, and add a
# `spec.postBuild.substituteFrom` referencing the `cluster-info` ConfigMap to
# `repos/gitops-system/clusters/template/kubecost.yaml` so `${CLUSTER_NAME}`
# resolves per cluster (same pattern as karpenter-config.yaml). This makes the
# clusterId cluster-specific, which is what kubecost expects for cost
# attribution across the multi-cluster fleet.
#
# FINDING 24b (live end-to-end run, extends FINDING 24). SUPERSEDED by the
# re-pin to 2.8.4 (see FINDING 16) — NO LONGER APPLICABLE, kept as history.
# This finding applied ONLY to the 2.9.x transition line. Setting
# `global.clusterId` alone was NOT sufficient for cost-analyzer 2.9.x. After
# `global.clusterId` was set, install still failed at
# cost-analyzer-deployment-template.yaml:1062 with "Kubecost 2.9.x is only
# used for preparing agents to upgrade to 3.0. In kubecost 2.9, cluster_id is
# set in two places." The `kubecost.clusterId` helper
# (cost-analyzer/templates/_helpers.tpl, chart v2.9.6) required
# `.Values.global.clusterId` to EQUAL
# `.Values.prometheus.server.global.external_labels.cluster_id`, else it
# `fail`ed the render. The chart default for the second key is `cluster-one`
# (values.yaml), which mismatches our `${CLUSTER_NAME}`. The 2.9-only fix was
# to also set `prometheus.server.global.external_labels.cluster_id`. This was
# the two-place requirement that identified 2.9.x as a transition release;
# rather than satisfy it, the fleet re-pinned to the stable 2.8.4 line, which
# has no such requirement, so `prometheus.server.global.external_labels
# .cluster_id` has been REMOVED from kubecost-release.yaml.
#
# FINDING 25 (live end-to-end run, task 8.4 correction, area D). The
# aws-load-balancer-controller 3.5.0 (controller v3) chart FAILS at startup on
# this solution's workload clusters: the pod crash-loops with "failed to get
# VPC ID ... ec2imds GetMetadata ... context deadline exceeded". Root cause:
# the Crossplane-composed workload managed NodeGroup runs with the EC2 IMDS
# hop limit of 1, so controller pods (one network hop from the host) cannot
# reach IMDS to discover the VPC id. Controller v2 (chart 1.4.6) tolerated a
# missing IMDS VPC lookup; v3 hard-fails. This is a v2->v3 behaviour change the
# offline values-schema diff could not see. Resolution (no node/IMDS change):
# supply the controller its region and VPC id explicitly through the chart
# values `region` and `vpcId`, sourced per-cluster from the workload
# `cluster-info` ConfigMap via a `spec.postBuild.substituteFrom` added to
# `repos/gitops-system/clusters/template/aws-load-balancer-controller.yaml`
# (same pattern as karpenter-config.yaml / kubecost.yaml). `region` reads the
# ConfigMap's `AWS_REGION` key and `vpcId` reads a NEW `VPC_ID` key added to
# both workload cluster-info ConfigMaps in
# `tools-config/crossplane-eks-composition/composition.yaml`; `VPC_ID` is
# populated from the composed VPC's `status.atProvider.vpcId`, surfaced onto
# the composite as `status.vpc_id` and mapped by the `cluster-info-mappings`
# patchSet like the other data keys. The fix also required declaring
# `status.vpc_id` (type string) in the XRD's status schema
# (`compositeresourcedefinition.yaml`); without it Crossplane drops the
# ToCompositeFieldPath write and the Required ConfigMap patch blocks with
# "Unsynced resources".
#
# FINDING 26 (live end-to-end run, area D). The EKS 1.35 workload cluster
# ships NO default StorageClass. The in-tree `gp2` default that older EKS
# provided is gone: the only StorageClass present is a non-default `gp2` still
# naming the REMOVED in-tree provisioner `kubernetes.io/aws-ebs`, which no
# longer provisions on 1.35. Consequence: PVC-backed add-ons whose PVCs leave
# `storageClassName` empty — kubecost `cost-analyzer` and its bundled
# `prometheus-server` — bind to nothing and their PVCs stay Pending, so the
# pods never start. Resolution (no node/driver change beyond what is already
# deployed): add a default `gp3` StorageClass backed by the EBS CSI driver
# (`provisioner: ebs.csi.aws.amazonaws.com`, `type: gp3`,
# `volumeBindingMode: WaitForFirstConsumer`, `allowVolumeExpansion: true`,
# `reclaimPolicy: Delete`, annotated
# `storageclass.kubernetes.io/is-default-class: "true"`) as
# `repos/gitops-system/tools/aws-ebs-csi/storageclass-gp3.yaml`, added to the
# `resources` list of that directory's `kustomization.yaml`. The EBS CSI
# HelmRelease that backs it is already deployed from the same directory, so
# the StorageClass rides the existing tools sync with no new dependency. This
# is an environment behaviour the offline version scan could not see; it pins
# no artifact version and adds no Version_Reference, so no `entries` row is
# recorded for it.
#
# FINDING 27 (live end-to-end run, area C/D). provider-aws v0.59.0's DynamoDB
# Table observe / isUpToDate path calls read actions beyond the create/update
# set. A workload Crossplane `Table` managed resource stayed Synced=False with
# `AccessDeniedException: not authorized to perform:
# dynamodb:DescribeContinuousBackups`, even though the table itself reached
# ACTIVE in AWS — the create succeeded, but drift detection could not read the
# table back. The embedded IAM policy in
# `repos/gitops-system/tools-config/crossplane-iam/crossplane-iam.yaml`
# (statement Sid Stmt1658117635374) granted only CreateTable, DeleteTable,
# DescribeTable, TagResource, UntagResource, UpdateTable. Resolution: added the
# three read actions the v0.59.0 Table observer makes —
# `dynamodb:DescribeContinuousBackups` (the one in the error),
# `dynamodb:DescribeTimeToLive`, and `dynamodb:ListTagsOfResource` — added all
# three proactively so successive observe calls do not fail one after another.
# Task 8.5 had left crossplane-iam unchanged; this is an environment behaviour
# the offline scan could not see. It pins no artifact version and adds no
# Version_Reference, so no `entries` row is recorded for it.
#
# FINDING 28 (live end-to-end run, area G — two automated-setup defects). The
# automated setup path (`initial-setup/auto/cfn.yaml`) deployed live surfaced
# two defects, both now fixed in the tracked repository. Neither pins an
# artifact version, so neither adds an `entries` row; recorded here because
# both are consequences of the task 10.3/10.4 Cloud9 → VS Code
# Dev_Environment migration.
#   a. `~/environment` WORKSPACE DIR NOT CREATED. The retained setup SSM
#      documents run as `OS_USER=ec2-user` and `cd /home/$OS_USER/environment`
#      (and `cd ~/environment` inside their heredoc scripts). Cloud9 used to
#      auto-create `~/environment`; the VS Code Dev_Environment's
#      CodeEditorSSMDoc creates only `${DevEnvironmentHomeFolder}` (`/workshop`)
#      for the `participant` user, so `/home/ec2-user/environment` never
#      exists. Result: `cd: /home/ec2-user/environment: No such file or
#      directory`, then `git clone` fails with "could not create work tree dir
#      ... Permission denied", cascading to the dependent documents. This is
#      the same Cloud9-workspace assumption FINDING 8 flags for the OS guard —
#      an assumption task 10.3/10.4 did not carry into the new instance. FIX:
#      each of the five documents that use `~/environment` (`CloneWorkshopRepo`,
#      `CreateRootSealedSecretsEncryptionKeysDoc`, `SetupCodeCommitSSHAccessDoc`,
#      `CloneCodeCommitReposDoc`, `CreateIAMRoleForCrossplaneDoc`) now runs
#      `mkdir -p /home/$OS_USER/environment && chown $OS_USER:
#      /home/$OS_USER/environment` immediately after its `case`/`esac`
#      OS_USER block and before the first `cd` — a self-contained,
#      order-independent per-document step.
#   b. CODEBUILD MASKED SETUP FAILURES. In the `BuildProject` BuildSpec
#      `build` phase, both callers captured setup status with command
#      substitution: `EXIT_CODE=$(process_command_status ...)`.
#      `process_command_status` echoes progress to stdout and signals failure
#      via `return`, so `$(...)` captured the progress TEXT rather than the
#      numeric return code; the failure was lost,
#      `CODEBUILD_BUILD_SUCCEEDING` stayed 1, and `post_build` signalled
#      SUCCESS to the WaitCondition — the stack reported CREATE_COMPLETE even
#      when the SSM setup commands had failed. FIX (both the
#      Configure-Workshop-Environment and Bootstrap-Git-And-Management-Cluster
#      call sites): call `process_command_status` directly, capture its return
#      code with `EXIT_CODE=$?`, and `exit $EXIT_CODE` (not `return`) on
#      non-zero so the build command terminates non-zero → CodeBuild marks the
#      build failed → `post_build` signals FAILURE → the stack rolls back.
#      `process_command_status`'s own logic is unchanged.

entries:

  # -----------------------------------------------------------------------
  # A. Kubernetes and Amazon EKS cluster versions
  # -----------------------------------------------------------------------

  - component: Management_Cluster Kubernetes version
    category: k8s-version
    file: initial-setup/config/mgmt-cluster-eksctl.yaml
    locator: metadata.version
    current: '1.29'
    target: '1.36'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    notes: >-
      Target_Kubernetes_Version. Workload clusters stay one minor behind at
      1.35 by intent, so the documented cluster upgrade scenario has a real
      version gap to exercise.

  - component: Workload_Cluster control plane Kubernetes version
    category: k8s-version
    file: repos/gitops-system/clusters-config/template/def/eks-cluster.yaml
    locator: spec.parameters.eks-k8s-version
    current: '1.28'
    target: '1.35'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: workload-k8s-version
    notes: >-
      Workload_Kubernetes_Version. Requirement 2 criterion 4 requires the
      managed node group version to equal this, so the two form a duplicate
      group.

  - component: Workload_Cluster managed node group Kubernetes version
    category: k8s-version
    file: repos/gitops-system/clusters-config/template/def/eks-cluster.yaml
    locator: spec.parameters.mng-k8s-version
    current: '1.28'
    target: '1.35'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: workload-k8s-version

  - component: CloudFormation KubernetesVersion parameter default
    category: k8s-version
    file: initial-setup/auto/cfn.yaml
    locator: Parameters.KubernetesVersion.Default (line 16)
    current: '1.29'
    target: '1.36'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: target-k8s-version

  - component: CloudFormation KubernetesVersion AllowedValues
    category: k8s-version
    file: initial-setup/auto/cfn.yaml
    locator: Parameters.KubernetesVersion.AllowedValues[0] (line 18)
    current: '1.29'
    target: '1.36'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: cfn-k8s-allowed-value-0
    notes: >-
      Requirement 2 criterion 5. FINDING 3: two further allowedValues lists
      inside SSM documents state the same three versions and must be
      rewritten identically.

  - component: CloudFormation KubernetesVersion AllowedValues
    category: k8s-version
    file: initial-setup/auto/cfn.yaml
    locator: Parameters.KubernetesVersion.AllowedValues[1] (line 19)
    current: '1.28'
    target: '1.35'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: cfn-k8s-allowed-value-1

  - component: CloudFormation KubernetesVersion AllowedValues
    category: k8s-version
    file: initial-setup/auto/cfn.yaml
    locator: Parameters.KubernetesVersion.AllowedValues[2] (line 20)
    current: '1.27'
    target: '<unresolved — task 6.2: the remaining Amazon EKS standard-support minor versions; all three allowedValues lists are rewritten together and their length is not preserved>'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: cfn-k8s-allowed-value-2

  - component: InstallK8sClientToolsDoc Version allowedValues
    category: k8s-version
    file: initial-setup/auto/cfn.yaml
    locator: Resources.InstallK8sClientToolsDoc...parameters.Version.allowedValues[0] (line 131)
    current: '1.29'
    target: '1.36'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: cfn-k8s-allowed-value-0
    notes: >-
      FINDING 3. Named in no requirements or design table. SSM rejects a
      value CloudFormation accepted unless this list is rewritten with the
      parameter's.

  - component: InstallK8sClientToolsDoc Version allowedValues
    category: k8s-version
    file: initial-setup/auto/cfn.yaml
    locator: Resources.InstallK8sClientToolsDoc...parameters.Version.allowedValues[1] (line 132)
    current: '1.28'
    target: '1.35'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: cfn-k8s-allowed-value-1

  - component: InstallK8sClientToolsDoc Version allowedValues
    category: k8s-version
    file: initial-setup/auto/cfn.yaml
    locator: Resources.InstallK8sClientToolsDoc...parameters.Version.allowedValues[2] (line 133)
    current: '1.27'
    target: '<unresolved — task 6.2: the remaining Amazon EKS standard-support minor versions; all three allowedValues lists are rewritten together and their length is not preserved>'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: cfn-k8s-allowed-value-2

  - component: CreateEKSClusterDoc Version allowedValues
    category: k8s-version
    file: initial-setup/auto/cfn.yaml
    locator: Resources.CreateEKSClusterDoc...parameters.Version.allowedValues[0] (line 375)
    current: '1.29'
    target: '1.36'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: cfn-k8s-allowed-value-0
    notes: >-
      FINDING 3.

  - component: CreateEKSClusterDoc Version allowedValues
    category: k8s-version
    file: initial-setup/auto/cfn.yaml
    locator: Resources.CreateEKSClusterDoc...parameters.Version.allowedValues[1] (line 376)
    current: '1.28'
    target: '1.35'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: cfn-k8s-allowed-value-1

  - component: CreateEKSClusterDoc Version allowedValues
    category: k8s-version
    file: initial-setup/auto/cfn.yaml
    locator: Resources.CreateEKSClusterDoc...parameters.Version.allowedValues[2] (line 377)
    current: '1.27'
    target: '<unresolved — task 6.2: the remaining Amazon EKS standard-support minor versions; all three allowedValues lists are rewritten together and their length is not preserved>'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: cfn-k8s-allowed-value-2

  - component: ConfigureWorkshopEnvironmentDoc Version default
    category: k8s-version
    file: initial-setup/auto/cfn.yaml
    locator: Resources.ConfigureWorkshopEnvironmentDoc...parameters.Version.default (line 888)
    current: '1.22'
    target: '1.36'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: target-k8s-version
    notes: >-
      FINDING 4. A fourth, stale Kubernetes version, absent from every
      AllowedValues list and from every requirements and design table.
      CodeBuild always passes Version explicitly, so the default is dead
      today — but it is a Version_Reference and cfn-lint will not flag it.

  - component: Workload_Cluster Kubernetes version in upgrade scenario
    category: k8s-version
    file: scenarios.md
    locator: line 342 — 'from `1.23`'
    current: '1.23'
    target: '1.35'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: scenario-k8s-from
    notes: >-
      FINDING 12. scenarios.md states 1.23 -> 1.24, drifted from
      eks-cluster.yaml's 1.28 before this upgrade began. Design area I
      requires the scenario to read 1.35 -> 1.36 and to state no other
      Kubernetes version number.

  - component: Workload_Cluster Kubernetes version in upgrade scenario
    category: k8s-version
    file: scenarios.md
    locator: line 342 — 'to `1.24`'
    current: '1.24'
    target: '1.36'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: scenario-k8s-to

  - component: Workload_Cluster Kubernetes version in upgrade scenario
    category: k8s-version
    file: scenarios.md
    locator: line 362 — 'from 1.23'
    current: '1.23'
    target: '1.35'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: scenario-k8s-from

  - component: Workload_Cluster Kubernetes version in upgrade scenario
    category: k8s-version
    file: scenarios.md
    locator: line 362 — 'to 1.24'
    current: '1.24'
    target: '1.36'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/kubernetes-versions-standard.html
    consulted: 2026-11-20
    duplicate_group: scenario-k8s-to


  # -----------------------------------------------------------------------
  # B. Flux distribution — controller images and version label
  # -----------------------------------------------------------------------

  - component: flux distribution
    category: container-image
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: metadata.labels."app.kubernetes.io/version" — 35 occurrences (lines 11, 22, 42, 62, 81, 101, 191, 217, 242, 262, 297, 815, 2016, 2612, 3136, 3499, 3510, 3531, 3630, 5262, 5273, 5359, 6286, 6297, 6383, 6820, 7231, 7897, 7908, 7929, 7950)
    current: v2.1.2
    target: v2.9.5
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-distribution-version
    notes: >-
      The documented granularity exception — see SCAN SCOPE. Check 9 asserts
      that every occurrence of this label in this file equals the target.

  - component: flux distribution
    category: container-image
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: metadata.labels."app.kubernetes.io/version" — 35 occurrences (lines 11, 22, 42, 62, 81, 101, 191, 217, 242, 262, 297, 815, 2016, 2612, 3136, 3499, 3510, 3531, 3630, 5262, 5273, 5359, 6286, 6297, 6383, 6820, 7231, 7897, 7908, 7929, 7950)
    current: v2.1.2
    target: v2.9.5
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-distribution-version

  - component: flux-source-controller
    category: container-image
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.template.spec.containers[0].image (ghcr.io/fluxcd/source-controller, line 3566)
    current: v1.1.2
    target: v1.9.5
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-source-controller-image

  - component: flux-source-controller
    category: container-image
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.template.spec.containers[0].image (ghcr.io/fluxcd/source-controller, line 3566)
    current: v1.1.2
    target: v1.9.5
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-source-controller-image

  - component: flux-kustomize-controller
    category: container-image
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.template.spec.containers[0].image (ghcr.io/fluxcd/kustomize-controller, line 5302)
    current: v1.1.1
    target: v1.9.5
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomize-controller-image

  - component: flux-kustomize-controller
    category: container-image
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.template.spec.containers[0].image (ghcr.io/fluxcd/kustomize-controller, line 5302)
    current: v1.1.1
    target: v1.9.5
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomize-controller-image

  - component: flux-helm-controller
    category: container-image
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.template.spec.containers[0].image (ghcr.io/fluxcd/helm-controller, line 6326)
    current: v0.36.2
    target: v1.6.4
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-helm-controller-image

  - component: flux-helm-controller
    category: container-image
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.template.spec.containers[0].image (ghcr.io/fluxcd/helm-controller, line 6326)
    current: v0.36.2
    target: v1.6.4
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-helm-controller-image

  - component: flux-notification-controller
    category: container-image
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.template.spec.containers[0].image (ghcr.io/fluxcd/notification-controller, line 7978)
    current: v1.1.0
    target: v1.9.4
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-notification-controller-image

  - component: flux-notification-controller
    category: container-image
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.template.spec.containers[0].image (ghcr.io/fluxcd/notification-controller, line 7978)
    current: v1.1.0
    target: v1.9.4
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-notification-controller-image


  # -----------------------------------------------------------------------
  # C. Crossplane platform — chart, packages, helper image, thumbprint
  # -----------------------------------------------------------------------

  - component: crossplane
    category: helm-chart
    file: repos/gitops-system/tools/crossplane/crossplane-core/crossplane-release.yaml
    locator: spec.chart.spec.version (line 12)
    current: 1.15.0
    target: 1.20.12
    status: upgrade
    source_url: https://charts.crossplane.io/stable
    consulted: 2026-11-20
    notes: >-
      Stays on the v1.20 line. Crossplane v2 removes composite resource
      connection details, which the workload cluster Flux bootstrap depends
      on; the v2 migration is a documented non-goal. Pinned as a single
      exact version — no range or wildcard.

  - component: crossplane-contrib/provider-aws
    category: crossplane-package
    file: repos/gitops-system/tools/crossplane/crossplane-aws-provider/aws-provider.yaml
    locator: spec.package (line 21)
    current: xpkg.upbound.io/crossplane-contrib/provider-aws:v0.47.1
    target: xpkg.upbound.io/crossplane-contrib/provider-aws:v0.59.0
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    notes: >-
      NOT superseded. The monolith published v0.59.0 on 12 August 2026; the
      2023 project-status reading was stale. Staying on it keeps every
      *.aws.crossplane.io group-version and the aws.crossplane.io/v1beta1
      ProviderConfig unchanged. Exactly one AWS provider package in the
      finished tree.

  - component: crossplane-contrib/provider-kubernetes
    category: crossplane-package
    file: repos/gitops-system/tools/crossplane/crossplane-k8s-provider/k8s-provider.yaml
    locator: spec.package (line 17)
    current: xpkg.upbound.io/crossplane-contrib/provider-kubernetes:v0.13.0
    target: xpkg.upbound.io/crossplane-contrib/provider-kubernetes:v1.3.1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-kubernetes/releases/tag/v1.3.1
    consulted: 2026-11-20

  - component: kubectl helper image
    category: container-image
    file: repos/gitops-system/tools/crossplane/crossplane-k8s-provider-config/k8s-providerconfig.yaml
    locator: spec.forProvider.manifest.spec.template.spec.containers[0].image (line 32)
    current: bitnami/kubectl:1.22.11
    target: alpine/k8s:1.36.0
    status: upgrade
    source_url: https://hub.docker.com/r/alpine/k8s/tags
    consulted: 2026-11-20
    duplicate_group: bitnami-kubectl-image
    notes: >-
      Two occurrences in one file, both inside Object manifests (a Job and a
      CronJob-style template). Both must state the same tag.

  - component: kubectl helper image
    category: container-image
    file: repos/gitops-system/tools/crossplane/crossplane-k8s-provider-config/k8s-providerconfig.yaml
    locator: spec.forProvider.manifest...spec.template.spec.containers[0].image (line 75)
    current: bitnami/kubectl:1.22.11
    target: alpine/k8s:1.36.0
    status: upgrade
    source_url: https://hub.docker.com/r/alpine/k8s/tags
    consulted: 2026-11-20
    duplicate_group: bitnami-kubectl-image

  - component: openid-connect-provider thumbprint
    category: aws-resource
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: spec.resources[cluster-oidc-idp].base.spec.forProvider.thumbprintList[0] (line 606)
    current: 9e99a48a9960b14926bb7f3b02e22da2b0ab7280
    target: 9e99a48a9960b14926bb7f3b02e22da2b0ab7280
    status: upgrade
    source_url: https://raw.githubusercontent.com/crossplane-contrib/provider-aws/v0.59.0/package/crds/iam.aws.crossplane.io_openidconnectproviders.yaml
    consulted: 2026-11-24
    notes: >-
      RESOLVED — documented well-known constant (supersedes DERIVATION 2).
      target equals current, so per FINDING 15 the residual `upgrade` flag
      applies: none of the five conditional flags fires, a source of record
      exists and is recorded in source_url, and no edit to the value is
      required at the occurrence. `no-source` was rejected because a source
      of record does exist (the pinned CRD plus the documented AWS root CA)
      and adopting it would force source_url to be omitted; `removed` no
      longer applies because the value is retained, not eliminated.

      DERIVATION 2 (patch) IS SUPERSEDED. A live end-to-end run against a
      real AWS account showed DERIVATION 2 produced an AWS-invalid value:
      the task 4.4 patch drove thumbprintList[0] from
      status.eks_cluster_oidc_issuer_url — the issuer URL (~60 chars) — but
      IAM requires each thumbprintList member to be exactly a 40-char hex
      SHA-1 thumbprint and rejected it with "Member must have length equal
      to 40". Crossplane patches cannot compute a SHA-1, and the CRD in
      provider-aws v0.59.0 lists thumbprintList in `required` with
      minItems: 1 (so the field cannot be omitted or left empty).

      RESOLUTION. thumbprintList[0] is set to the documented well-known AWS
      OIDC root-CA thumbprint constant 9e99a48a9960b14926bb7f3b02e22da2b0ab7280
      (Starfield Services Root CA G2 — the repo's original value). For EKS
      OIDC endpoints AWS validates against its own trusted-CA store and does
      not rely on the supplied value; it only enforces the 40-char format.
      The Starfield root CA is long-lived, so a stale-fingerprint failure is
      not a practical risk. The invalid thumbprint patch is removed; the
      status.eks_cluster_oidc_issuer_url -> spec.forProvider.url patch is
      kept. Requirement 4.17 was relaxed to permit this single documented
      well-known constant (with the comment recorded in the composition)
      while still forbidding per-cluster or hand-copied thumbprints that
      could silently go stale.


  # -----------------------------------------------------------------------
  # D. Cluster add-ons — Helm charts and coupled version labels
  # -----------------------------------------------------------------------

  - component: aws-load-balancer-controller
    category: helm-chart
    file: repos/gitops-system/tools/aws-load-balancer-controller/aws-lb-controller-release.yaml
    locator: spec.chart.spec.version (line 13)
    current: 1.4.6
    target: 3.5.0
    status: upgrade
    source_url: https://aws.github.io/eks-charts
    consulted: 2026-11-20
    notes: >-
      High risk. Chart and appVersion are now aligned at 3.x, so this spans
      a controller major bump (v2 -> v3). Expect values-schema renames and
      new IAM actions.

  - component: aws-ebs-csi-driver
    category: helm-chart
    file: repos/gitops-system/tools/aws-ebs-csi/aws-ebs-csi-release.yaml
    locator: spec.chart.spec.version (line 13)
    current: 2.30.0
    target: 2.66.0
    status: upgrade
    source_url: https://kubernetes-sigs.github.io/aws-ebs-csi-driver
    consulted: 2026-11-20
    notes: >-
      Same major line.

  - component: external-secrets
    category: helm-chart
    file: repos/gitops-system/tools/external-secrets/external-secrets-release.yaml
    locator: spec.chart.spec.version (line 15)
    current: 0.4.4
    target: 2.10.0
    status: upgrade
    source_url: https://charts.external-secrets.io
    consulted: 2026-11-20
    notes: >-
      High risk — two major lines crossed. FINDING 6: this bump also forces
      an API migration off external-secrets.io/v1alpha1, which is scoped
      nowhere in the requirements or design.

  - component: sealed-secrets
    category: helm-chart
    file: repos/gitops-system/tools/sealed-secrets/sealed-secrets-release.yaml
    locator: spec.chart.spec.version (line 15)
    current: 2.7.1
    target: 2.20.0
    status: upgrade
    source_url: https://bitnami-labs.github.io/sealed-secrets
    consulted: 2026-11-20
    notes: >-
      Same major line. The controller image version this chart deploys
      drives the namespace label and the kubeseal install, which are a
      duplicate group with it.

  - component: karpenter
    category: helm-chart
    file: repos/gitops-system/tools/karpenter/karpenter-release.yaml
    locator: spec.chart.spec.version (line 13)
    current: 0.36.1
    target: 1.14.1
    status: upgrade
    source_url: https://karpenter.sh/
    consulted: 2026-11-20
    notes: >-
      High risk — v1 API graduation with documented breaking changes. See
      the karpenter.sh and karpenter.k8s.aws group-version entries.

  - component: kubecost cost-analyzer
    category: helm-chart
    file: repos/gitops-system/tools/kubecost/kubecost-release.yaml
    locator: spec.chart.spec.version (line 15)
    current: 2.2.2
    target: 2.8.4
    status: upgrade
    source_url: https://gallery.ecr.aws/kubecost/cost-analyzer
    consulted: 2026-11-20
    notes: >-
      Exact string, no wildcard. FINDING 16: there is no `8.15.x` line of this
      chart. The cost-analyzer stream is 1.x then 2.x; `8.15.X` in the Helm
      index is the version constraint on the bundled `prometheus` SUBCHART of
      the 1.x cost-analyzer charts, which the design misread as the chart's own
      line. The target was first recorded as 2.9.6 (the highest cost-analyzer
      tag resolvable through this repository's `kubecost-repository`,
      `oci://public.ecr.aws/kubecost`), but the live end-to-end run proved the
      entire 2.9.x line is a "prepare to upgrade to 3.0" TRANSITION release: it
      hard-requires federation/transition config (cluster_id in two places —
      FINDING 24b — plus a global federated store) and is not a valid
      standalone fresh-install target. Re-pinned to 2.8.4, the highest STABLE
      non-transition cost-analyzer in that OCI registry (2.8.x tops out at
      2.8.4). FINDING 24 (`global.clusterId`) still applies and remains set —
      it is valid and harmless in 2.8.x — but FINDING 24b (the 2.9-only second
      cluster_id place) is superseded and its value removed. Verification check
      3 resolves each pinned chart against the index of the HelmRepository its
      HelmRelease references; 2.8.4 is present there.

  - component: kubecost global.clusterId / global.clusterName
    category: helm-values
    file: repos/gitops-system/tools/kubecost/kubecost-release.yaml
    locator: spec.values.global.clusterId, spec.values.global.clusterName
    current: '<absent in 2.2.2 — not required>'
    target: ${CLUSTER_NAME}
    status: upgrade
    source_url: https://gallery.ecr.aws/kubecost/cost-analyzer
    consulted: 2026-09-15
    notes: >-
      FINDING 24 (live end-to-end run). The kubecost 2.x line requires
      `global.clusterId` — a new MANDATORY value the 2.2.2 chart did not
      require (its bundled `finopsagent` subchart aborts install with
      "clusterId is required. Please set .Values.global.clusterId"). The
      offline values-schema diff in task 8.4 could not catch it because
      kubecost publishes no `values.schema.json`. Still applies at the re-pinned
      target 2.8.4 and remains set. Value `${CLUSTER_NAME}`, resolved
      per-cluster by the `spec.postBuild.substituteFrom` on the `cluster-info`
      ConfigMap added to repos/gitops-system/clusters/template/kubecost.yaml
      (same pattern as karpenter-config.yaml). `${CLUSTER_NAME}` is a Flux
      substitution variable, not a static pin, so no upstream version resolves
      it. (FINDING 24b — the additional 2.9-only second cluster_id place — is
      superseded by the re-pin to 2.8.4; see the entry below.)

  - component: kubecost prometheus.server.global.external_labels.cluster_id
    category: helm-values
    file: repos/gitops-system/tools/kubecost/kubecost-release.yaml
    locator: spec.values.prometheus.server.global.external_labels.cluster_id
    current: '<removed — 2.9-transition-only, not present at target 2.8.4>'
    target: '<n/a — SUPERSEDED by re-pin to 2.8.4; entry removed from kubecost-release.yaml>'
    status: superseded
    source_url: https://github.com/kubecost/cost-analyzer-helm-chart/blob/v2.9.6/cost-analyzer/templates/_helpers.tpl
    consulted: 2026-11-20
    notes: >-
      FINDING 24b (live end-to-end run) — SUPERSEDED, kept as history. This was
      the SECOND of the two places cost-analyzer 2.9.x required cluster_id: the
      `kubecost.clusterId` helper (chart v2.9.6 _helpers.tpl) `fail`ed the
      render — "In kubecost 2.9, cluster_id is set in two places"
      (cost-analyzer-deployment-template.yaml:1062) — unless `global.clusterId`
      EQUALED `prometheus.server.global.external_labels.cluster_id`. That
      two-place requirement is exactly what marks 2.9.x as a 3.0-transition
      release. Rather than satisfy it, the fleet re-pinned to the stable 2.8.4
      line (FINDING 16), which has no such requirement. The
      `prometheus.server.global.external_labels.cluster_id` value has therefore
      been REMOVED from kubecost-release.yaml (leaving the now-empty
      prometheus.server.global block removed too); only the prometheus image
      repository overrides remain under prometheus.server.

  - component: aws-load-balancer-controller region / vpcId
    category: helm-values
    file: repos/gitops-system/tools/aws-load-balancer-controller/aws-lb-controller-release.yaml
    locator: spec.values.region, spec.values.vpcId
    current: '<absent in 1.4.6 — controller v2 discovered these via IMDS>'
    target: ${AWS_REGION} / ${VPC_ID}
    status: upgrade
    source_url: https://github.com/aws/eks-charts/blob/master/stable/aws-load-balancer-controller/README.md
    consulted: 2026-11-20
    notes: >-
      FINDING 25 (live end-to-end run). Controller v3 (chart 3.5.0) hard-fails
      at startup fetching the VPC id from IMDS ("failed to get VPC ID ...
      ec2imds GetMetadata ... context deadline exceeded") because the workload
      managed NodeGroup runs with IMDS hop limit 1. Controller v2 (chart 1.4.6)
      tolerated this. Supply region/vpcId explicitly via chart values. Resolved
      per-cluster by a `spec.postBuild.substituteFrom` on the cluster-info
      ConfigMap added to
      repos/gitops-system/clusters/template/aws-load-balancer-controller.yaml
      (same pattern as karpenter-config.yaml). `region` reads the ConfigMap's
      `AWS_REGION`; `vpcId` reads the new `VPC_ID` key. Both are Flux
      substitution variables, not static pins.

  - component: cluster-info VPC_ID data key
    category: aws-resource
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: spec.resources[configmap-cluster-info-workload-cluster / remote-configmap-cluster-info-workload-cluster].base.spec.forProvider.manifest.data.VPC_ID
    current: '<absent>'
    target: status.atProvider.vpcId (of the composed VPC)
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/blob/v0.59.0/apis/ec2/v1beta1/vpc_types.go
    consulted: 2026-11-20
    notes: >-
      FINDING 25 (live end-to-end run). New `VPC_ID` data key added to BOTH
      workload cluster-info ConfigMaps so aws-load-balancer-controller v3.5.0
      can be given the VPC id without reaching IMDS. Populated from the composed
      VPC managed resource's `status.atProvider.vpcId` (confirmed the field
      that carries the vpc-xxxx id in provider-aws v0.59.0 VPCObservation),
      surfaced onto the composite as `status.vpc_id` by a ToCompositeFieldPath
      patch on the vpc resource, then mapped into the ConfigMap data by the
      `cluster-info-mappings` patchSet like the other data keys.

  - component: kubecost-modeling
    category: container-image
    file: repos/gitops-system/tools/kubecost/kubecost-release.yaml
    locator: spec.values.forecasting.fullImageName (line 39)
    current: v0.1.6
    target: v0.1.31
    status: upgrade
    source_url: https://gallery.ecr.aws/kubecost/kubecost-modeling
    consulted: 2026-11-20
    notes: >-
      The only tagged image reference in this HelmRelease; the six others
      state a repository with no tag and are not Version_References. Tag chosen
      to match the re-pinned cost-analyzer 2.8.4 (FINDING 16): `helm show
      values oci://public.ecr.aws/kubecost/cost-analyzer --version 2.8.4`
      declares `forecasting.image.tag: v0.1.31` (default repo
      gcr.io/kubecost1/kubecost-modeling). This repo mirrors every kubecost
      image to public.ecr.aws, so the `fullImageName` override is KEPT and
      re-pinned to `public.ecr.aws/kubecost/kubecost-modeling:v0.1.31` (tag
      confirmed present on the public ECR mirror). The previous value
      `v0.1.34` was the 2.9.6-era modeling tag.

  - component: sealed-secrets controller version label
    category: k8s-version
    file: repos/gitops-system/clusters-config/template/secrets/namespace.yaml
    locator: metadata.labels."app.kubernetes.io/version" (line 7)
    current: v0.26.3
    target: '<unresolved — task 8.4: the full major.minor.patch of the sealed-secrets controller image that chart 2.20.0 deploys, recorded in canonical BARE form with no leading `v`; occurrences that write it `v`-prefixed say so in their own notes (Requirement 5 criterion 11 constrains the label to the full version, criterion 12 constrains kubeseal to major.minor) — see FINDING 18>'
    status: upgrade
    source_url: https://github.com/bitnami-labs/sealed-secrets/releases
    consulted: 2026-11-20
    duplicate_group: sealed-secrets-controller-version
    notes: >-
      Requirement 5 criterion 11. The category is k8s-version by the label's
      own semantics; it is the Sealed Secrets controller version, not a
      cluster version. NOTATION: this occurrence writes the version
      `v`-prefixed (`v0.26.3`), while the group `target` is bare, so
      verification check 9 must strip one leading `v` here before comparing.
      GROUP MEMBERSHIP (task 1.3 audit, FINDING 18): the group is this label
      plus the three kubeseal occurrences in initial-setup/README.md — four
      occurrences of one version stream. It is NOT the "triple" the design
      describes: the sealed-secrets CHART pin (2.7.1 → 2.20.0) is a
      different version stream and cannot share one identical target with a
      controller version, so it is deliberately excluded and is coupled to
      this label by Requirement 5 criterion 11 instead.


  # -----------------------------------------------------------------------
  # E. Node OS — AL2 -> AL2023
  # -----------------------------------------------------------------------

  - component: managed node group AMI type (non-gpu)
    category: aws-resource
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: spec.resources[eks-nodegroup].patches[].transforms[].map.non-gpu (line 577)
    current: AL2_x86_64
    target: AL2023_x86_64_STANDARD
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/al2023.html
    consulted: 2026-11-20
    notes: >-
      Amazon EKS ceased publishing Amazon Linux 2 AMIs on 26 November 2025;
      1.32 was the last version published. Both 1.36 and 1.35 are above that
      boundary, so AL2 is unusable for both cluster tiers. The map transform
      structure and both branches are preserved.

  - component: managed node group AMI type (gpu)
    category: aws-resource
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: spec.resources[eks-nodegroup].patches[].transforms[].map.gpu (line 576)
    current: AL2_x86_64_GPU
    target: AL2023_x86_64_NVIDIA
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/APIReference/API_Nodegroup.html
    consulted: 2026-09-15
    notes: >-
      Resolved by task 1.3, and consumed verbatim by task 5.1. The source
      is the EKS API reference because that page carries the enum of
      strings the `Nodegroup` `amiType` field accepts, which is the exact
      form task 5.1 must write:
      AL2023_x86_64_STANDARD | AL2023_ARM_64_STANDARD |
      AL2023_x86_64_NEURON | AL2023_x86_64_NVIDIA | AL2023_ARM_64_NVIDIA.
      Publication for BOTH cluster tiers was verified, not assumed: the
      recommended-AMI SSM parameters
      /aws/service/eks/optimized-ami/1.36/amazon-linux-2023/x86_64/nvidia/recommended/image_id
      and the identical path at 1.35 both resolve to an AMI ID
      (us-west-2, 2026-09-15). FINDING 17: `AL2_x86_64_GPU` served NVIDIA
      GPU and AWS Neuron instances from one AMI; AL2023 splits them, so
      this single branch cannot cover both. NVIDIA is chosen because the
      composition's `workload-type: gpu` branch names GPU;
      AL2023_x86_64_NEURON exists and is published for 1.36 and 1.35 too,
      but selecting it is a separate parameter value, not this edit.

  - component: Karpenter node AMI family
    category: aws-resource
    file: repos/gitops-system/tools-config/karpenter-config/node-pool.yaml
    locator: EC2NodeClass.spec.amiFamily (line 39)
    current: AL2
    target: AL2023
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/al2023.html
    consulted: 2026-11-20
    notes: >-
      Changes together with spec.amiSelectorTerms, which Karpenter v1 makes
      required (`- alias: al2023@latest`) and which is absent today.
      amiFamily: AL2023 selects the nodeadm bootstrap mode.


  # -----------------------------------------------------------------------
  # F. CLI tooling and kubectl artifact selection
  # -----------------------------------------------------------------------

  - component: kubectl
    category: cli-tool
    file: initial-setup/auto/cfn.yaml
    locator: Resources.InstallK8sClientToolsDoc...runCommand case branch — K8S_VERSION (line 180)
    current: 1.29.0
    target: '<unresolved — task 6.2: a released 1.36 patch>'
    status: upgrade
    source_url: https://kubernetes.io/releases/
    consulted: 2026-11-20
    notes: >-
      FINDING 2: this bash `case` inside an SSM document, not a
      CloudFormation `Mappings` block, is what selects the kubectl artifact.
      Verification check 12 and Property 9 must be restated against these
      branches.

  - component: kubectl
    category: cli-tool
    file: initial-setup/auto/cfn.yaml
    locator: Resources.InstallK8sClientToolsDoc...runCommand case branch — K8S_VERSION (line 184)
    current: 1.28.5
    target: '<unresolved — task 6.2: a released 1.35 patch>'
    status: upgrade
    source_url: https://kubernetes.io/releases/
    consulted: 2026-11-20

  - component: kubectl
    category: cli-tool
    file: initial-setup/auto/cfn.yaml
    locator: Resources.InstallK8sClientToolsDoc...runCommand case branch — K8S_VERSION (line 188)
    current: 1.27.9
    target: '<unresolved — task 6.2: a released patch of the third allowed minor>'
    status: upgrade
    source_url: https://kubernetes.io/releases/
    consulted: 2026-11-20

  - component: kubectl artifact release date
    category: cli-tool
    file: initial-setup/auto/cfn.yaml
    locator: Resources.InstallK8sClientToolsDoc...runCommand case branch — RELEASE_DATE (line 181)
    current: '2024-01-04'
    target: '<unresolved — task 6.2: the S3 release-date path segment paired with the target kubectl patch>'
    status: upgrade
    source_url: https://kubernetes.io/releases/
    consulted: 2026-11-20
    duplicate_group: kubectl-release-date-cfn
    notes: >-
      Part of the kubectl download URL
      (amazon-eks.s3.../$K8S_VERSION/$RELEASE_DATE/...), so it must move
      with the patch version or the download 404s.

  - component: kubectl artifact release date
    category: cli-tool
    file: initial-setup/auto/cfn.yaml
    locator: Resources.InstallK8sClientToolsDoc...runCommand case branch — RELEASE_DATE (line 185)
    current: '2024-01-04'
    target: '<unresolved — task 6.2: the S3 release-date path segment paired with the target kubectl patch>'
    status: upgrade
    source_url: https://kubernetes.io/releases/
    consulted: 2026-11-20
    duplicate_group: kubectl-release-date-cfn

  - component: kubectl artifact release date
    category: cli-tool
    file: initial-setup/auto/cfn.yaml
    locator: Resources.InstallK8sClientToolsDoc...runCommand case branch — RELEASE_DATE (line 189)
    current: '2024-01-04'
    target: '<unresolved — task 6.2: the S3 release-date path segment paired with the target kubectl patch>'
    status: upgrade
    source_url: https://kubernetes.io/releases/
    consulted: 2026-11-20
    duplicate_group: kubectl-release-date-cfn

  - component: flux CLI
    category: cli-tool
    file: initial-setup/auto/cfn.yaml
    locator: Resources.InstallK8sClientToolsDoc...runCommand — FLUX_VERSION (line 266)
    current: 0.35.0
    target: 2.9.5
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-cli-version
    notes: >-
      Requirement 3 criteria 6 and 7 constrain major.minor only; 2.9.5
      matches the pinned distribution. FINDING 1: this is the ONLY place the
      Flux CLI is pinned.

  - component: flux CLI
    category: cli-tool
    file: initial-setup/README.md
    locator: line 145 — `curl -s https://fluxcd.io/install.sh | sudo bash`
    current: '<unpinned — no version string; install.sh resolves to the latest release at run time>'
    target: 2.9.5
    status: mutable
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-cli-version
    notes: >-
      FINDING 1, confirmed by direct read. Design area B, task 1.3 and task
      12.1 all describe a Flux CLI pair stating 0.35.0 in both cfn.yaml and
      this file. This file pins nothing. `current` records the command
      actually present rather than a version string that is not there. The
      duplicate_group is retained so check 9 catches divergence once task
      12.1 pins it, and so Requirement 3 criteria 6/7 have both occurrences
      to compare.

  - component: eksctl
    category: cli-tool
    file: initial-setup/auto/cfn.yaml
    locator: Resources.InstallK8sClientToolsDoc...runCommand — eksctl download URL (line 248)
    current: latest
    target: '<unresolved — task 6.1/12.1: an exact eksctl release; design area A names v0.230.0 as the version whose config API is targeted>'
    status: mutable
    source_url: https://github.com/eksctl-io/eksctl/releases
    consulted: 2026-11-20
    duplicate_group: eksctl-cli-version
    notes: >-
      FINDING 10. `/releases/latest/download/` resolves at run time, so the
      installed eksctl is not reproducible and may not accept the eksctl.io
      config API version the repository declares (Requirement 2 criterion
      10).

  - component: eksctl
    category: cli-tool
    file: initial-setup/README.md
    locator: line 165 — eksctl download URL
    current: latest
    target: '<unresolved — task 6.1/12.1: an exact eksctl release; design area A names v0.230.0 as the version whose config API is targeted>'
    status: mutable
    source_url: https://github.com/eksctl-io/eksctl/releases
    consulted: 2026-11-20
    duplicate_group: eksctl-cli-version
    notes: >-
      FINDING 10.

  - component: kubectl
    category: cli-tool
    file: initial-setup/README.md
    locator: line 138 — kubectl download URL, version path segment
    current: 1.24.7
    target: 1.36.2
    status: upgrade
    source_url: https://kubernetes.io/releases/
    consulted: 2026-11-20
    notes: >-
      Released 31 October 2022. Requirement 9 criterion 17 requires this to
      equal its inventory target, and Requirement 2 criterion 12 ties the
      installed client minor to the cluster version.

  - component: kubectl artifact release date
    category: cli-tool
    file: initial-setup/README.md
    locator: line 138 — kubectl download URL, release-date path segment
    current: '2022-10-31'
    target: '2026-07-05'
    status: upgrade
    source_url: https://kubernetes.io/releases/
    consulted: 2026-11-20

  - component: yq
    category: cli-tool
    file: initial-setup/README.md
    locator: line 98 — yq release download URL
    current: v4.24.5
    target: v4.53.6
    status: upgrade
    source_url: https://github.com/mikefarah/yq/releases
    consulted: 2026-11-20
    notes: >-
      cfn.yaml installs yq by resolving the latest release tag from a
      redirect and states no version, so this is a single-occurrence
      reference rather than a pair — the two install paths disagree by
      construction.

  - component: kubeseal
    category: cli-tool
    file: initial-setup/README.md
    locator: line 150 — release tag path segment
    current: v0.19.4
    target: '<unresolved — task 8.4: the full major.minor.patch of the sealed-secrets controller image that chart 2.20.0 deploys, recorded in canonical BARE form with no leading `v`; occurrences that write it `v`-prefixed say so in their own notes (Requirement 5 criterion 11 constrains the label to the full version, criterion 12 constrains kubeseal to major.minor) — see FINDING 18>'
    status: upgrade
    source_url: https://github.com/bitnami-labs/sealed-secrets/releases
    consulted: 2026-11-20
    duplicate_group: sealed-secrets-controller-version
    notes: >-
      Three occurrences of the same version in two adjacent lines, all of
      which must be edited together. NOTATION: this occurrence is the GitHub
      release tag and is `v`-prefixed (`v0.19.4`), while the group `target`
      is bare, so verification check 9 must strip one leading `v` here
      before comparing. The two tarball file names on lines 150 and 151 are
      bare and compare directly. FINDING 18.

  - component: kubeseal
    category: cli-tool
    file: initial-setup/README.md
    locator: line 150 — tarball file name
    current: 0.19.4
    target: '<unresolved — task 8.4: the full major.minor.patch of the sealed-secrets controller image that chart 2.20.0 deploys, recorded in canonical BARE form with no leading `v`; occurrences that write it `v`-prefixed say so in their own notes (Requirement 5 criterion 11 constrains the label to the full version, criterion 12 constrains kubeseal to major.minor) — see FINDING 18>'
    status: upgrade
    source_url: https://github.com/bitnami-labs/sealed-secrets/releases
    consulted: 2026-11-20
    duplicate_group: sealed-secrets-controller-version
    notes: >-
      NOTATION: bare, so it compares directly against the group `target`.
      FINDING 18.

  - component: kubeseal
    category: cli-tool
    file: initial-setup/README.md
    locator: line 151 — tarball file name
    current: 0.19.4
    target: '<unresolved — task 8.4: the full major.minor.patch of the sealed-secrets controller image that chart 2.20.0 deploys, recorded in canonical BARE form with no leading `v`; occurrences that write it `v`-prefixed say so in their own notes (Requirement 5 criterion 11 constrains the label to the full version, criterion 12 constrains kubeseal to major.minor) — see FINDING 18>'
    status: upgrade
    source_url: https://github.com/bitnami-labs/sealed-secrets/releases
    consulted: 2026-11-20
    duplicate_group: sealed-secrets-controller-version
    notes: >-
      NOTATION: bare, so it compares directly against the group `target`.
      FINDING 18.

  - component: external-secrets documentation link
    category: helm-chart
    file: README.md
    locator: README.md — https://external-secrets.io/latest/provider/aws-secrets-manager/
    current: v0.5.7
    target: latest
    status: upgrade
    source_url: https://external-secrets.io/latest/provider/aws-secrets-manager/
    consulted: 2026-11-20
    notes: >-
      FINDING 11, resolved by task 12.2. The pre-upgrade URL pinned the
      external-secrets docs to v0.5.7, whose path no longer resolves after
      the 0.4.4 → 2.10.0 chart bump. External Secrets versions its docs by
      operator release, not by chart version, so the durable resolvable
      form is the unversioned `latest` channel:
      https://external-secrets.io/latest/provider/aws-secrets-manager/
      (verified reachable 2026-11-20). The version token in the URL is
      therefore `latest`, which is what the documentation-drift check
      (verification check 11) compares this entry's `target` against.


  # -----------------------------------------------------------------------
  # G. Bootstrap environment — removed Cloud9 pins, and untabled runtime pins
  # -----------------------------------------------------------------------

  - component: Cloud9ImageId
    category: aws-resource
    file: initial-setup/auto/cfn.yaml
    locator: Parameters.Cloud9ImageId.Default (line 42)
    current: amazonlinux-2-x86_64
    target: n/a
    status: removed
    successor: Dev_Environment AMI parameter (VS Code on Amazon EC2)
    source_url: https://docs.aws.amazon.com/cli/latest/reference/cloud9/
    consulted: 2026-11-20
    duplicate_group: cloud9-image-id
    notes: >-
      AWS Cloud9 is closed to new customers, so AWS::Cloud9::EnvironmentEC2
      fails stack creation in a fresh account. Removed together with the
      Cloud9 environment rather than upgraded.

  - component: Cloud9ImageId
    category: aws-resource
    file: initial-setup/auto/cfn.yaml
    locator: Parameters.Cloud9ImageId.AllowedValues[0] (line 44)
    current: amazonlinux-2-x86_64
    target: n/a
    status: removed
    successor: Dev_Environment AMI parameter (VS Code on Amazon EC2)
    source_url: https://docs.aws.amazon.com/cli/latest/reference/cloud9/
    consulted: 2026-11-20
    duplicate_group: cloud9-image-id

  - component: Cloud9ImageId
    category: aws-resource
    file: initial-setup/auto/cfn.yaml
    locator: Parameters.Cloud9ImageId.AllowedValues[1] (line 45)
    current: ubuntu-18.04-x86_64
    target: n/a
    status: removed
    successor: Dev_Environment AMI parameter (VS Code on Amazon EC2)
    source_url: https://docs.aws.amazon.com/cli/latest/reference/cloud9/
    consulted: 2026-11-20
    duplicate_group: cloud9-image-id
    notes: >-
      Ubuntu 18.04 reached end of standard support in May 2023. Removed with
      the parameter.

  - component: Dev_Environment operating system in documentation
    category: aws-resource
    file: initial-setup/README.md
    locator: line 53 — '*Ubuntu 18.04* platform'
    current: Ubuntu 18.04
    target: n/a
    status: removed
    successor: the Dev_Environment OS name and version (AL2023)
    source_url: https://ubuntu.com/about/release-cycle
    consulted: 2026-11-20
    notes: >-
      Requirement 2 criterion 20 and design area I. The whole 'Create and
      prepare the Cloud9 workspace' section is replaced in task 12.1.

  - component: InstallK8sClientToolsDoc Amazon Linux major guard
    category: aws-resource
    file: initial-setup/auto/cfn.yaml
    locator: Resources.InstallK8sClientToolsDoc...runCommand — `if [[ "$OS_VER" != "2" && "$OS_VER" != "2023" ]]` (lines 160-164)
    current: '2'
    target: '2023'
    status: upgrade
    source_url: https://docs.aws.amazon.com/eks/latest/userguide/al2023.html
    consulted: 2026-11-20
    notes: >-
      FINDING 8. This retained-and-retargeted document exits 103 on any
      Amazon Linux other than 2. The Dev_Environment runs AL2023, so as
      written the retargeted document fails immediately. It also sets
      OS_USER from this branch, so the guard cannot simply be deleted.
      RESOLVED by task 10.4: the guard now accepts both Amazon Linux 2
      (OS_VER 2) and Amazon Linux 2023 (OS_VER 2023) —
      `if [[ "$OS_VER" != "2" && "$OS_VER" != "2023" ]]; then ... exit 103`.
      OS_USER stays ec2-user, which is correct on both AL2 and AL2023, so
      the retargeted document now runs on the AL2023 Dev_Environment.

  - component: CodeBuild build image
    category: container-image
    file: initial-setup/auto/cfn.yaml
    locator: Resources.BuildProject.Properties.Environment.Image (line 1262)
    current: aws/codebuild/amazonlinux2-x86_64-standard:3.0
    target: aws/codebuild/amazonlinux-x86_64-standard:5.0
    status: upgrade
    source_url: https://docs.aws.amazon.com/codebuild/latest/userguide/build-env-ref-available.html
    consulted: 2026-11-20
    notes: >-
      FINDING 9. Recorded in no requirements or design table. The AL2
      standard:3.0 image is superseded by later AL2/AL2023 standard images.
      SCOPED to task 10.4, not 10.3: the CodeBuild image is coupled to the
      BuildProject buildspec, whose `runtime-versions: python: 3.8` and
      instance-discovery variables (currently the undefined CLOUD9_* vars)
      task 10.4 rewrites when it retargets the Run Command to the
      Dev_Environment instance. Bumping the image without the coupled
      buildspec runtime version would leave an inconsistent intermediate that
      10.3 does not own. Task 10.3 resolved only the standalone
      TriggerBuildLambda runtime (see entry above). RESOLVED by task 10.4:
      image bumped to aws/codebuild/amazonlinux-x86_64-standard:5.0 (the
      AL2023 x86_64 standard managed image; the amazonlinux2 -> amazonlinux
      alias rename of 2024-11-22 keeps the old alias valid), and the coupled
      buildspec `runtime-versions: python` bumped 3.8 -> 3.12. The buildspec
      Cloud9 instance-discovery variables ($CLOUD9_INSTANCE_ID,
      $CLOUD9_EBS_VOLUME_SIZE, $CLOUD9_INSTANCE_ROLE_ARN) were replaced with
      $DEV_ENVIRONMENT_INSTANCE_ID (from the DevEnvironmentInstance stack
      reference) and $CLI_ROLE (EKSEnvironmentRole ARN); the VolumeSize
      parameter and volume-resize step were dropped (Req 2.19).

  - component: TriggerBuildLambda runtime
    category: aws-resource
    file: initial-setup/auto/cfn.yaml
    locator: Resources.TriggerBuildLambda.Properties.Runtime (line 1463)
    current: python3.8
    target: python3.13
    status: upgrade
    source_url: https://docs.aws.amazon.com/lambda/latest/dg/lambda-runtimes.html
    consulted: 2026-11-20
    notes: >-
      FINDING 9. Recorded in no requirements or design table. python3.8 is
      past Lambda's deprecation for creating new functions, so this stack
      cannot be created as written even after Cloud9 is removed. Design area
      G lists CustomTriggerBuild/TriggerBuildLambda as Unchanged, which
      cannot hold. RESOLVED by task 10.3: TriggerBuildLambda.Runtime bumped
      python3.8 -> python3.13 (the same currently-supported runtime the added
      Dev_Environment helper Lambdas use). This clears cfn-lint W2531. The
      TriggerBuildLambda handler code is runtime-agnostic (boto3 + urllib3)
      and needs no change.


  # -----------------------------------------------------------------------
  # H. Sample applications — base images, dependency pins, image tags
  # -----------------------------------------------------------------------

  - component: python base image
    category: container-image
    file: repos/apps/product-catalog-api/v1/Dockerfile
    locator: FROM (line 4)
    current: python:3.9-slim
    target: python:3.12-slim
    status: upgrade
    source_url: https://devguide.python.org/versions/
    consulted: 2026-11-20
    duplicate_group: python-base-image
    notes: >-
      CPython 3.9 reached end of security support in October 2025.

  - component: python base image
    category: container-image
    file: repos/apps/product-catalog-api/v2/Dockerfile
    locator: FROM (line 4)
    current: python:3.9-slim
    target: python:3.12-slim
    status: upgrade
    source_url: https://devguide.python.org/versions/
    consulted: 2026-11-20
    duplicate_group: python-base-image

  - component: node base image
    category: container-image
    file: repos/apps/product-catalog-fe/Dockerfile
    locator: FROM (line 3)
    current: node:14
    target: node:22
    status: upgrade
    source_url: https://nodejs.org/en/about/previous-releases
    consulted: 2026-11-20
    notes: >-
      Node.js 14 reached end of life in April 2023. `node:14` is also a
      floating major tag, so the replacement should be a specific major at
      minimum.

  - component: flask-restx
    category: python-dist
    file: repos/apps/product-catalog-api/v1/requirements.txt
    locator: flask-restx (line 1)
    current: 1.3.0
    target: 1.3.0
    status: upgrade
    source_url: https://pypi.org/project/flask-restx/
    consulted: 2026-11-20
    duplicate_group: api-flask-restx-version

  - component: Flask
    category: python-dist
    file: repos/apps/product-catalog-api/v1/requirements.txt
    locator: Flask (line 2)
    current: 3.1.3
    target: 3.1.3
    status: upgrade
    source_url: https://pypi.org/project/Flask/
    consulted: 2026-11-20
    duplicate_group: api-flask-version
    notes: >-
      Requirement 7 criterion 6 needs one identical version in v1 and v2; v2
      currently states 3.0.3. The shared lines must end up byte-identical.

  - component: werkzeug
    category: python-dist
    file: repos/apps/product-catalog-api/v1/requirements.txt
    locator: werkzeug (line 3)
    current: 3.1.6
    target: 3.1.6
    status: upgrade
    source_url: https://pypi.org/project/werkzeug/
    consulted: 2026-11-20
    duplicate_group: api-werkzeug-version
    notes: >-
      v2 currently states 3.0.6. Same byte-identical constraint as Flask.

  - component: gunicorn
    category: python-dist
    file: repos/apps/product-catalog-api/v1/requirements.txt
    locator: gunicorn (line 4)
    current: 23.0.0
    target: 23.0.0
    status: upgrade
    source_url: https://pypi.org/project/gunicorn/
    consulted: 2026-11-20
    duplicate_group: api-gunicorn-version

  - component: requests
    category: python-dist
    file: repos/apps/product-catalog-api/v1/requirements.txt
    locator: requests (line 5)
    current: v2.33.0
    target: 2.34.2
    status: unresolvable
    source_url: https://pypi.org/project/requests/
    consulted: 2026-11-20
    duplicate_group: api-requests-version
    notes: >-
      Stray `v` prefix; PyPI publishes no such version. Fails installation
      today, independently of any upgrade. Requirement 1 criterion 4 retains
      `current` verbatim.

  - component: flask-cors
    category: python-dist
    file: repos/apps/product-catalog-api/v1/requirements.txt
    locator: flask-cors (line 6)
    current: 6.0.0
    target: 6.0.0
    status: upgrade
    source_url: https://pypi.org/project/flask-cors/
    consulted: 2026-11-20
    duplicate_group: api-flask-cors-version

  - component: boto3
    category: python-dist
    file: repos/apps/product-catalog-api/v1/requirements.txt
    locator: boto3 (line 7)
    current: 1.35.39
    target: 1.35.39
    status: upgrade
    source_url: https://pypi.org/project/boto3/
    consulted: 2026-11-20
    duplicate_group: api-boto3-version

  - component: markupsafe
    category: python-dist
    file: repos/apps/product-catalog-api/v1/requirements.txt
    locator: markupsafe (line 8)
    current: 2.1.5
    target: 2.1.5
    status: upgrade
    source_url: https://pypi.org/project/markupsafe/
    consulted: 2026-11-20
    duplicate_group: api-markupsafe-version

  - component: flask-restx
    category: python-dist
    file: repos/apps/product-catalog-api/v2/requirements.txt
    locator: flask-restx (line 1)
    current: 1.3.0
    target: 1.3.0
    status: upgrade
    source_url: https://pypi.org/project/flask-restx/
    consulted: 2026-11-20
    duplicate_group: api-flask-restx-version

  - component: Flask
    category: python-dist
    file: repos/apps/product-catalog-api/v2/requirements.txt
    locator: Flask (line 2)
    current: 3.0.3
    target: 3.1.3
    status: upgrade
    source_url: https://pypi.org/project/Flask/
    consulted: 2026-11-20
    duplicate_group: api-flask-version

  - component: werkzeug
    category: python-dist
    file: repos/apps/product-catalog-api/v2/requirements.txt
    locator: werkzeug (line 3)
    current: 3.0.6
    target: 3.1.6
    status: upgrade
    source_url: https://pypi.org/project/werkzeug/
    consulted: 2026-11-20
    duplicate_group: api-werkzeug-version

  - component: gunicorn
    category: python-dist
    file: repos/apps/product-catalog-api/v2/requirements.txt
    locator: gunicorn (line 4)
    current: 23.0.0
    target: 23.0.0
    status: upgrade
    source_url: https://pypi.org/project/gunicorn/
    consulted: 2026-11-20
    duplicate_group: api-gunicorn-version

  - component: requests
    category: python-dist
    file: repos/apps/product-catalog-api/v2/requirements.txt
    locator: requests (line 5)
    current: v2.33.0
    target: 2.34.2
    status: unresolvable
    source_url: https://pypi.org/project/requests/
    consulted: 2026-11-20
    duplicate_group: api-requests-version

  - component: flask-cors
    category: python-dist
    file: repos/apps/product-catalog-api/v2/requirements.txt
    locator: flask-cors (line 6)
    current: 6.0.0
    target: 6.0.0
    status: upgrade
    source_url: https://pypi.org/project/flask-cors/
    consulted: 2026-11-20
    duplicate_group: api-flask-cors-version

  - component: boto3
    category: python-dist
    file: repos/apps/product-catalog-api/v2/requirements.txt
    locator: boto3 (line 7)
    current: 1.35.39
    target: 1.35.39
    status: upgrade
    source_url: https://pypi.org/project/boto3/
    consulted: 2026-11-20
    duplicate_group: api-boto3-version

  - component: markupsafe
    category: python-dist
    file: repos/apps/product-catalog-api/v2/requirements.txt
    locator: markupsafe (line 8)
    current: 2.1.5
    target: 2.1.5
    status: upgrade
    source_url: https://pypi.org/project/markupsafe/
    consulted: 2026-11-20
    duplicate_group: api-markupsafe-version

  - component: axios
    category: npm-package
    file: repos/apps/product-catalog-fe/package.json
    locator: dependencies.axios (line 13)
    current: ^1.18.0
    target: ^1.18.0
    status: upgrade
    source_url: https://registry.npmjs.org/axios
    consulted: 2026-11-20
    notes: >-
      Verify the lower bound is published; the range must resolve with the
      npm client bundled in the target Node image.

  - component: body-parser
    category: npm-package
    file: repos/apps/product-catalog-fe/package.json
    locator: dependencies.body-parser (line 14)
    current: ^1.20.8
    target: ^1.20.3
    status: unresolvable
    source_url: https://registry.npmjs.org/body-parser
    consulted: 2026-11-20
    notes: >-
      1.20.8 has not been published on npm. Fails installation today.
      Requirement 1 criterion 4 retains `current` verbatim.

  - component: ejs
    category: npm-package
    file: repos/apps/product-catalog-fe/package.json
    locator: dependencies.ejs (line 15)
    current: ^3.1.10
    target: ^3.1.10
    status: upgrade
    source_url: https://registry.npmjs.org/ejs
    consulted: 2026-11-20

  - component: express
    category: npm-package
    file: repos/apps/product-catalog-fe/package.json
    locator: dependencies.express (line 16)
    current: ^4.22.3
    target: ^4.21.2
    status: unresolvable
    source_url: https://registry.npmjs.org/express
    consulted: 2026-11-20
    notes: >-
      4.22.3 has not been published on npm. Fails installation today.

  - component: prom-client
    category: npm-package
    file: repos/apps/product-catalog-fe/package.json
    locator: dependencies.prom-client (line 17)
    current: ^14.0.1
    target: ^14.0.1
    status: upgrade
    source_url: https://registry.npmjs.org/prom-client
    consulted: 2026-11-20

  - component: nodemon
    category: npm-package
    file: repos/apps/product-catalog-fe/package.json
    locator: devDependencies.nodemon (line 20)
    current: ^2.0.18
    target: ^2.0.18
    status: upgrade
    source_url: https://registry.npmjs.org/nodemon
    consulted: 2026-11-20

  - component: product-catalog-fe image tag
    category: container-image
    file: repos/apps-manifests/product-catalog-fe-manifests/kubernetes/overlays/prod/deployment.yaml
    locator: spec.template.spec.containers[0].image (line 11)
    current: latest
    target: '<unresolved — task 11.3: an immutable release tag>'
    status: mutable
    source_url: https://kubernetes.io/docs/concepts/containers/images/
    consulted: 2026-11-20
    duplicate_group: product-catalog-fe-image-tag

  - component: product-catalog-fe image tag
    category: container-image
    file: repos/apps-manifests/product-catalog-fe-manifests/kubernetes/overlays/staging/deployment.yaml
    locator: spec.template.spec.containers[0].image (line 11)
    current: latest
    target: '<unresolved — task 11.3: an immutable release tag>'
    status: mutable
    source_url: https://kubernetes.io/docs/concepts/containers/images/
    consulted: 2026-11-20
    duplicate_group: product-catalog-fe-image-tag

  - component: product-catalog-api image tag
    category: container-image
    file: repos/apps-manifests/product-catalog-api-manifests/v1/kubernetes/overlays/prod/deployment.yaml
    locator: spec.template.spec.containers[0].image (line 10)
    current: v1
    target: v1
    status: no-source
    duplicate_group: product-catalog-api-image-tag-v1
    notes: >-
      The sample's own application image, built and pushed by the reader; no
      upstream registry states a version for it, so Requirement 1 criterion
      8 applies and target equals current. Checked per Requirement 7
      criterion 9: `v1`/`v2` are not `latest`, so no mutable-tag replacement
      is required here. They are still re-pushable tags, which is worth
      stating in the migration log.

  - component: product-catalog-api image tag
    category: container-image
    file: repos/apps-manifests/product-catalog-api-manifests/v1/kubernetes/overlays/staging/deployment.yaml
    locator: spec.template.spec.containers[0].image (line 10)
    current: v1
    target: v1
    status: no-source
    duplicate_group: product-catalog-api-image-tag-v1
    notes: >-
      The sample's own application image, built and pushed by the reader; no
      upstream registry states a version for it, so Requirement 1 criterion
      8 applies and target equals current. Checked per Requirement 7
      criterion 9: `v1`/`v2` are not `latest`, so no mutable-tag replacement
      is required here. They are still re-pushable tags, which is worth
      stating in the migration log.

  - component: product-catalog-api image tag
    category: container-image
    file: repos/apps-manifests/product-catalog-api-manifests/v2-staging/kubernetes/overlays/staging/deployment.yaml
    locator: spec.template.spec.containers[0].image (line 11)
    current: v2
    target: v2
    status: no-source


  # -----------------------------------------------------------------------
  # I. API group-versions — one entry per occurrence
  # -----------------------------------------------------------------------

  - component: eksctl ClusterConfig API version
    category: k8s-api-version
    file: initial-setup/config/mgmt-cluster-eksctl.yaml
    locator: apiVersion (line 2)
    current: eksctl.io/v1alpha5
    target: '<unresolved — task 6.1: the eksctl config API version accepted by the installed eksctl release; design area A names v0.230.0>'
    status: upgrade
    source_url: https://eksctl.io/usage/schema/
    consulted: 2026-11-20
    notes: >-
      Requirement 2 criterion 10. The accepted config API version is read
      from the eksctl release the Bootstrap_Setup installs — which is itself
      unpinned today (FINDING 10).

  - component: external-secrets.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/external-secrets/sealed-secrets-key.yaml
    locator: apiVersion (line 14)
    current: external-secrets.io/v1alpha1
    target: external-secrets.io/v1
    status: upgrade
    source_url: https://github.com/external-secrets/external-secrets/releases
    consulted: 2026-11-20
    duplicate_group: external-secrets-group-version
    notes: >-
      FINDING 6. External Secrets Operator is going from chart 0.4.4 to
      2.10.0 and external-secrets.io/v1alpha1 is not served by current ESO.
      This migration is scoped in neither the requirements nor the design;
      task 8.3 must absorb it. LIVE-RUN CORRECTION: the correct served
      version for ESO 2.10.0 is external-secrets.io/v1, not v1beta1. The
      earlier v1alpha1 -> v1beta1 migration was insufficient because ESO
      2.10.0 ships v1beta1 with served=false (v1 is the only served
      version). A Flux dry-run against the live cluster failed with
      `no matches for kind "ExternalSecret" in version
      "external-secrets.io/v1beta1"`, so both the SecretStore and
      ExternalSecret in sealed-secrets-key.yaml declare v1. The v1 schema
      is compatible with the existing field bodies.

  - component: external-secrets.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/external-secrets/sealed-secrets-key.yaml
    locator: apiVersion (line 29)
    current: external-secrets.io/v1alpha1
    target: external-secrets.io/v1
    status: upgrade
    source_url: https://github.com/external-secrets/external-secrets/releases
    consulted: 2026-11-20
    duplicate_group: external-secrets-group-version

  - component: helm.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/external-secrets.yaml
    locator: apiVersion (line 18)
    current: helm.toolkit.fluxcd.io/v2beta1
    target: helm.toolkit.fluxcd.io/v2
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-helmrelease-group-version
    notes: >-
      Flux v2.7 removed the v1beta1 group-versions and v2.8 removed
      source/kustomize v1beta2 and helm v2beta2. The target controllers
      serve exactly one version per CRD, so the finished tree must declare
      the GA version at every occurrence.

  - component: helm.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/sealed-secrets.yaml
    locator: apiVersion (line 17)
    current: helm.toolkit.fluxcd.io/v2beta1
    target: helm.toolkit.fluxcd.io/v2
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-helmrelease-group-version

  - component: helm.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/aws-ebs-csi/aws-ebs-csi-release.yaml
    locator: apiVersion (line 1)
    current: helm.toolkit.fluxcd.io/v2beta1
    target: helm.toolkit.fluxcd.io/v2
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-helmrelease-group-version

  - component: helm.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/aws-load-balancer-controller/aws-lb-controller-release.yaml
    locator: apiVersion (line 1)
    current: helm.toolkit.fluxcd.io/v2beta1
    target: helm.toolkit.fluxcd.io/v2
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-helmrelease-group-version

  - component: helm.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-core.yaml
    locator: apiVersion (line 20)
    current: helm.toolkit.fluxcd.io/v2beta1
    target: helm.toolkit.fluxcd.io/v2
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-helmrelease-group-version

  - component: helm.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-core/crossplane-release.yaml
    locator: apiVersion (line 1)
    current: helm.toolkit.fluxcd.io/v2beta1
    target: helm.toolkit.fluxcd.io/v2
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-helmrelease-group-version

  - component: helm.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/external-secrets/external-secrets-release.yaml
    locator: apiVersion (line 1)
    current: helm.toolkit.fluxcd.io/v2beta1
    target: helm.toolkit.fluxcd.io/v2
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-helmrelease-group-version

  - component: helm.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/karpenter/karpenter-release.yaml
    locator: apiVersion (line 1)
    current: helm.toolkit.fluxcd.io/v2beta1
    target: helm.toolkit.fluxcd.io/v2
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-helmrelease-group-version

  - component: helm.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/kubecost/kubecost-release.yaml
    locator: apiVersion (line 1)
    current: helm.toolkit.fluxcd.io/v2beta1
    target: helm.toolkit.fluxcd.io/v2
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-helmrelease-group-version

  - component: helm.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/sealed-secrets/sealed-secrets-release.yaml
    locator: apiVersion (line 1)
    current: helm.toolkit.fluxcd.io/v2beta1
    target: helm.toolkit.fluxcd.io/v2
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-helmrelease-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/crossplane-iam.yaml
    locator: apiVersion (line 1)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/eks-cluster.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/external-secrets-config.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/external-secrets-iam.yaml
    locator: apiVersion (line 1)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/external-secrets.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/flux.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/sealed-secrets.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/secrets.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/clusters-config.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/crossplane-eks-composition.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/crossplane.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/crossplane.yaml
    locator: spec.patches[].target.group + .version (line 24)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-sync.yaml
    locator: apiVersion (line 16)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/karpenter-config.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/karpenter-iam.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/karpenter.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/aws-auth.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/aws-ebs-csi-iam.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/aws-ebs-csi.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/aws-load-balancer-controller-iam.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/aws-load-balancer-controller.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/crossplane.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/crossplane.yaml
    locator: spec.patches[].target.group + .version (line 18)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-sync.yaml
    locator: apiVersion (line 16)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/karpenter-config.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/karpenter-iam.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/karpenter.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/kubecost.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/workloads.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-aws-provider-config.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-aws-provider.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-core.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-k8s-provider-config.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-k8s-provider.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/workloads/template/flux-kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: kustomize.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-workloads/template/app-template/flux-kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.toolkit.fluxcd.io/v1beta2
    target: kustomize.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-kustomization-group-version

  - component: source.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-sync.yaml
    locator: apiVersion (line 3)
    current: source.toolkit.fluxcd.io/v1beta2
    target: source.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-source-group-version

  - component: source.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-sync.yaml
    locator: apiVersion (line 3)
    current: source.toolkit.fluxcd.io/v1beta2
    target: source.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-source-group-version

  - component: source.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/aws-ebs-csi/aws-ebs-csi-repo.yaml
    locator: apiVersion (line 2)
    current: source.toolkit.fluxcd.io/v1beta2
    target: source.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-source-group-version

  - component: source.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/aws-load-balancer-controller/aws-lb-controller-repo.yaml
    locator: apiVersion (line 2)
    current: source.toolkit.fluxcd.io/v1beta2
    target: source.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-source-group-version

  - component: source.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-core/crossplane-repository.yaml
    locator: apiVersion (line 2)
    current: source.toolkit.fluxcd.io/v1beta2
    target: source.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-source-group-version

  - component: source.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/external-secrets/external-secrets-repository.yaml
    locator: apiVersion (line 2)
    current: source.toolkit.fluxcd.io/v1beta2
    target: source.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-source-group-version

  - component: source.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/karpenter/karpenter-repo.yaml
    locator: apiVersion (line 2)
    current: source.toolkit.fluxcd.io/v1beta2
    target: source.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-source-group-version

  - component: source.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/kubecost/kubecost-repository.yaml
    locator: apiVersion (line 2)
    current: source.toolkit.fluxcd.io/v1beta2
    target: source.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-source-group-version

  - component: source.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/sealed-secrets/sealed-secrets-repository.yaml
    locator: apiVersion (line 2)
    current: source.toolkit.fluxcd.io/v1beta2
    target: source.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-source-group-version

  - component: source.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-system/workloads/template/git-repo.yaml
    locator: apiVersion (line 1)
    current: source.toolkit.fluxcd.io/v1beta2
    target: source.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-source-group-version

  - component: source.toolkit.fluxcd.io group-version
    category: k8s-api-version
    file: repos/gitops-workloads/template/app-template/git-repo.yaml
    locator: apiVersion (line 1)
    current: source.toolkit.fluxcd.io/v1beta2
    target: source.toolkit.fluxcd.io/v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: flux-source-group-version

  - component: alerts.notification.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (alerts.notification.toolkit.fluxcd.io) (line 6404)
    current: v1beta1
    target: n/a
    status: removed
    successor: 'notification.toolkit.fluxcd.io/<unresolved — task 3.1: the single version Flux v2.9.5 serves for alerts.notification.toolkit.fluxcd.io>'
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-alerts-v1beta1
    notes: >-
      One served CRD version declared inside a wholly generated file.
      gotk-components.yaml is replaced in whole by the pinned distribution's
      output in task 3.1, so these are not independently editable; they are
      enumerated because Correctness Property 3 quantifies over the CRD API
      versions declared in the twins, and check 9 decides twin agreement
      from this inventory. A served beta version is flagged `removed` with
      the GA version as its successor, because v2.9.5 serves exactly one
      version per CRD; a served version that survives carries target equal
      to current.

  - component: alerts.notification.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (alerts.notification.toolkit.fluxcd.io) (line 6598)
    current: v1beta2
    target: n/a
    status: removed
    successor: 'notification.toolkit.fluxcd.io/<unresolved — task 3.1: the single version Flux v2.9.5 serves for alerts.notification.toolkit.fluxcd.io>'
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-alerts-v1beta2

  - component: alerts.notification.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (alerts.notification.toolkit.fluxcd.io) (line 6404)
    current: v1beta1
    target: n/a
    status: removed
    successor: 'notification.toolkit.fluxcd.io/<unresolved — task 3.1: the single version Flux v2.9.5 serves for alerts.notification.toolkit.fluxcd.io>'
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-alerts-v1beta1

  - component: alerts.notification.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (alerts.notification.toolkit.fluxcd.io) (line 6598)
    current: v1beta2
    target: n/a
    status: removed
    successor: 'notification.toolkit.fluxcd.io/<unresolved — task 3.1: the single version Flux v2.9.5 serves for alerts.notification.toolkit.fluxcd.io>'
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-alerts-v1beta2

  - component: buckets.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (buckets.source.toolkit.fluxcd.io) (line 321)
    current: v1beta1
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-buckets-v1beta1

  - component: buckets.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (buckets.source.toolkit.fluxcd.io) (line 554)
    current: v1beta2
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-buckets-v1beta2

  - component: buckets.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (buckets.source.toolkit.fluxcd.io) (line 321)
    current: v1beta1
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-buckets-v1beta1

  - component: buckets.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (buckets.source.toolkit.fluxcd.io) (line 554)
    current: v1beta2
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-buckets-v1beta2

  - component: gitrepositories.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (gitrepositories.source.toolkit.fluxcd.io) (line 841)
    current: v1
    target: v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-gitrepositories-v1

  - component: gitrepositories.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (gitrepositories.source.toolkit.fluxcd.io) (line 1249)
    current: v1beta1
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-gitrepositories-v1beta1

  - component: gitrepositories.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (gitrepositories.source.toolkit.fluxcd.io) (line 1581)
    current: v1beta2
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-gitrepositories-v1beta2

  - component: gitrepositories.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (gitrepositories.source.toolkit.fluxcd.io) (line 841)
    current: v1
    target: v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-gitrepositories-v1

  - component: gitrepositories.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (gitrepositories.source.toolkit.fluxcd.io) (line 1249)
    current: v1beta1
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-gitrepositories-v1beta1

  - component: gitrepositories.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (gitrepositories.source.toolkit.fluxcd.io) (line 1581)
    current: v1beta2
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-gitrepositories-v1beta2

  - component: helmcharts.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (helmcharts.source.toolkit.fluxcd.io) (line 2051)
    current: v1beta1
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-helmcharts-v1beta1

  - component: helmcharts.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (helmcharts.source.toolkit.fluxcd.io) (line 2306)
    current: v1beta2
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-helmcharts-v1beta2

  - component: helmcharts.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (helmcharts.source.toolkit.fluxcd.io) (line 2051)
    current: v1beta1
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-helmcharts-v1beta1

  - component: helmcharts.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (helmcharts.source.toolkit.fluxcd.io) (line 2306)
    current: v1beta2
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-helmcharts-v1beta2

  - component: helmreleases.helm.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (helmreleases.helm.toolkit.fluxcd.io) (line 5382)
    current: v2beta1
    target: n/a
    status: removed
    successor: helm.toolkit.fluxcd.io/v2
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-helmreleases-v2beta1

  - component: helmreleases.helm.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (helmreleases.helm.toolkit.fluxcd.io) (line 5382)
    current: v2beta1
    target: n/a
    status: removed
    successor: helm.toolkit.fluxcd.io/v2
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-helmreleases-v2beta1

  - component: helmrepositories.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (helmrepositories.source.toolkit.fluxcd.io) (line 2638)
    current: v1beta1
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-helmrepositories-v1beta1

  - component: helmrepositories.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (helmrepositories.source.toolkit.fluxcd.io) (line 2856)
    current: v1beta2
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-helmrepositories-v1beta2

  - component: helmrepositories.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (helmrepositories.source.toolkit.fluxcd.io) (line 2638)
    current: v1beta1
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-helmrepositories-v1beta1

  - component: helmrepositories.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (helmrepositories.source.toolkit.fluxcd.io) (line 2856)
    current: v1beta2
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-helmrepositories-v1beta2

  - component: kustomizations.kustomize.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (kustomizations.kustomize.toolkit.fluxcd.io) (line 3653)
    current: v1
    target: v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-kustomizations-v1

  - component: kustomizations.kustomize.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (kustomizations.kustomize.toolkit.fluxcd.io) (line 4139)
    current: v1beta1
    target: n/a
    status: removed
    successor: kustomize.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-kustomizations-v1beta1

  - component: kustomizations.kustomize.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (kustomizations.kustomize.toolkit.fluxcd.io) (line 4680)
    current: v1beta2
    target: n/a
    status: removed
    successor: kustomize.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-kustomizations-v1beta2

  - component: kustomizations.kustomize.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (kustomizations.kustomize.toolkit.fluxcd.io) (line 3653)
    current: v1
    target: v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-kustomizations-v1

  - component: kustomizations.kustomize.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (kustomizations.kustomize.toolkit.fluxcd.io) (line 4139)
    current: v1beta1
    target: n/a
    status: removed
    successor: kustomize.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-kustomizations-v1beta1

  - component: kustomizations.kustomize.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (kustomizations.kustomize.toolkit.fluxcd.io) (line 4680)
    current: v1beta2
    target: n/a
    status: removed
    successor: kustomize.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-kustomizations-v1beta2

  - component: ocirepositories.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (ocirepositories.source.toolkit.fluxcd.io) (line 3162)
    current: v1beta2
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-ocirepositories-v1beta2

  - component: ocirepositories.source.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (ocirepositories.source.toolkit.fluxcd.io) (line 3162)
    current: v1beta2
    target: n/a
    status: removed
    successor: source.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-ocirepositories-v1beta2

  - component: providers.notification.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (providers.notification.toolkit.fluxcd.io) (line 6841)
    current: v1beta1
    target: n/a
    status: removed
    successor: 'notification.toolkit.fluxcd.io/<unresolved — task 3.1: the single version Flux v2.9.5 serves for providers.notification.toolkit.fluxcd.io>'
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-providers-v1beta1

  - component: providers.notification.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (providers.notification.toolkit.fluxcd.io) (line 7024)
    current: v1beta2
    target: n/a
    status: removed
    successor: 'notification.toolkit.fluxcd.io/<unresolved — task 3.1: the single version Flux v2.9.5 serves for providers.notification.toolkit.fluxcd.io>'
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-providers-v1beta2

  - component: providers.notification.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (providers.notification.toolkit.fluxcd.io) (line 6841)
    current: v1beta1
    target: n/a
    status: removed
    successor: 'notification.toolkit.fluxcd.io/<unresolved — task 3.1: the single version Flux v2.9.5 serves for providers.notification.toolkit.fluxcd.io>'
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-providers-v1beta1

  - component: providers.notification.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (providers.notification.toolkit.fluxcd.io) (line 7024)
    current: v1beta2
    target: n/a
    status: removed
    successor: 'notification.toolkit.fluxcd.io/<unresolved — task 3.1: the single version Flux v2.9.5 serves for providers.notification.toolkit.fluxcd.io>'
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-providers-v1beta2

  - component: receivers.notification.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (receivers.notification.toolkit.fluxcd.io) (line 7252)
    current: v1
    target: v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-receivers-v1

  - component: receivers.notification.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (receivers.notification.toolkit.fluxcd.io) (line 7473)
    current: v1beta1
    target: n/a
    status: removed
    successor: notification.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-receivers-v1beta1

  - component: receivers.notification.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/gotk-components.yaml
    locator: spec.versions[].name (receivers.notification.toolkit.fluxcd.io) (line 7677)
    current: v1beta2
    target: n/a
    status: removed
    successor: notification.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-receivers-v1beta2

  - component: receivers.notification.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (receivers.notification.toolkit.fluxcd.io) (line 7252)
    current: v1
    target: v1
    status: upgrade
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-receivers-v1

  - component: receivers.notification.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (receivers.notification.toolkit.fluxcd.io) (line 7473)
    current: v1beta1
    target: n/a
    status: removed
    successor: notification.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-receivers-v1beta1

  - component: receivers.notification.toolkit.fluxcd.io served CRD version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/gotk-components.yaml
    locator: spec.versions[].name (receivers.notification.toolkit.fluxcd.io) (line 7677)
    current: v1beta2
    target: n/a
    status: removed
    successor: notification.toolkit.fluxcd.io/v1
    source_url: https://github.com/fluxcd/flux2/discussions/5572
    consulted: 2026-11-20
    duplicate_group: gotk-crd-receivers-v1beta2

  - component: karpenter.k8s.aws group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/karpenter-config.yaml
    locator: spec.patches[0].patch — apiVersion inside the inline strategic-merge patch body (line 27)
    current: karpenter.k8s.aws/v1beta1
    target: karpenter.k8s.aws/v1
    status: upgrade
    source_url: https://karpenter.sh/v1.0/upgrading/v1-migration/
    consulted: 2026-11-20
    duplicate_group: karpenter-ec2nodeclass-group-version
    notes: >-
      FINDING 7. This occurrence lives inside a YAML string block, so a
      YAML-path walk over the Kustomization does not see it; only a text
      scan does. It must move to karpenter.k8s.aws/v1 with the EC2NodeClass
      it patches, or the patch stops matching.

  - component: karpenter.k8s.aws group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/karpenter-config.yaml
    locator: spec.patches[0].patch — apiVersion inside the inline strategic-merge patch body (line 27)
    current: karpenter.k8s.aws/v1beta1
    target: karpenter.k8s.aws/v1
    status: upgrade
    source_url: https://karpenter.sh/v1.0/upgrading/v1-migration/
    consulted: 2026-11-20
    duplicate_group: karpenter-ec2nodeclass-group-version
    notes: >-
      FINDING 7. Same as the mgmt twin.

  - component: karpenter.k8s.aws group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/karpenter-config/node-pool.yaml
    locator: NodePool.spec.template.spec.nodeClassRef.apiVersion (line 25)
    current: karpenter.k8s.aws/v1beta1
    target: '<unresolved — task 8.1: the v1 nodeClassRef form — `group: karpenter.k8s.aws` with `kind` and `name`; the `apiVersion` key itself is removed>'
    status: upgrade
    source_url: https://karpenter.sh/v1.0/upgrading/v1-migration/
    consulted: 2026-11-20
    notes: >-
      Requirement 5 criterion 6 names this explicitly. It is a group-version
      stated INSIDE the NodePool rather than as the resource's own
      apiVersion, and v1 changes its shape rather than just its version:
      apiVersion is replaced by group. A grep on `apiVersion:` finds the
      string but a search for the resource's own API version does not.

  - component: karpenter.k8s.aws group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/karpenter-config/node-pool.yaml
    locator: EC2NodeClass.apiVersion (line 34)
    current: karpenter.k8s.aws/v1beta1
    target: karpenter.k8s.aws/v1
    status: upgrade
    source_url: https://karpenter.sh/v1.0/upgrading/v1-migration/
    consulted: 2026-11-20
    duplicate_group: karpenter-ec2nodeclass-group-version

  - component: karpenter.sh group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/karpenter-config/node-pool.yaml
    locator: NodePool.apiVersion (line 1)
    current: karpenter.sh/v1beta1
    target: karpenter.sh/v1
    status: upgrade
    source_url: https://karpenter.sh/v1.0/upgrading/v1-migration/
    consulted: 2026-11-20

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: bin/add-app-cluster-overlay.sh
    locator: apiVersion (line 15)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version
    notes: >-
      Target equals current — kustomize still accepts
      kustomize.config.k8s.io/v1beta1, so no edit is required at any
      occurrence in this group. FINDING 15 settles the flag: `upgrade` is
      the residual flag and covers the already-current class, so it is
      correct here rather than a placeholder.

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/apps-manifests/product-catalog-api-manifests/v1/kubernetes/base/kustomization.yaml
    locator: apiVersion (line 1)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/apps-manifests/product-catalog-api-manifests/v1/kubernetes/overlays/prod/kustomization.yaml
    locator: apiVersion (line 1)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/apps-manifests/product-catalog-api-manifests/v1/kubernetes/overlays/staging/kustomization.yaml
    locator: apiVersion (line 1)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/apps-manifests/product-catalog-api-manifests/v2-staging/kubernetes/overlays/staging/kustomization.yaml
    locator: apiVersion (line 1)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/apps-manifests/product-catalog-fe-manifests/kubernetes/base/kustomization.yaml
    locator: apiVersion (line 1)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/apps-manifests/product-catalog-fe-manifests/kubernetes/overlays/prod/kustomization.yaml
    locator: apiVersion (line 1)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/apps-manifests/product-catalog-fe-manifests/kubernetes/overlays/staging/kustomization.yaml
    locator: apiVersion (line 1)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/def/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/secrets/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/flux-system/kustomization.yaml
    locator: apiVersion (line 1)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/clusters/mgmt/kustomization.yaml
    locator: apiVersion (line 1)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/flux-system/kustomization.yaml
    locator: apiVersion (line 1)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/kustomization.yaml
    locator: apiVersion (line 1)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/aws-auth/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/aws-ebs-csi-iam/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/aws-load-balancer-controller-iam/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/karpenter-config/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/karpenter-iam/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools/aws-ebs-csi/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools/aws-load-balancer-controller/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-aws-provider-config/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-aws-provider/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-core/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-k8s-provider-config/kustomization.yaml
    locator: apiVersion (line 1)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-k8s-provider/kustomization.yaml
    locator: apiVersion (line 1)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools/external-secrets/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools/karpenter/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools/kubecost/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/tools/sealed-secrets/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-system/workloads/template/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-workloads/template/app-template/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: kustomize Kustomization config API version
    category: k8s-api-version
    file: repos/gitops-workloads/template/kustomization.yaml
    locator: apiVersion (line 2)
    current: kustomize.config.k8s.io/v1beta1
    target: kustomize.config.k8s.io/v1beta1
    status: upgrade
    source_url: https://kubectl.docs.kubernetes.io/references/kustomize/kustomization/
    consulted: 2026-11-20
    duplicate_group: kustomize-config-api-version

  - component: EKSCluster composite API version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/def/eks-cluster.yaml
    locator: apiVersion (line 2)
    current: gitops.k8s.aws/v1beta1
    target: gitops.k8s.aws/v1beta1
    status: no-source
    duplicate_group: eks-cluster-composite-api-version
    notes: >-
      Defined by this repository's own compositeresourcedefinition.yaml, not
      by any upstream artifact, so Requirement 1 criterion 8 applies: no
      source of record and target equals current. See FINDING 14.

  - component: EKSCluster composite API version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 13)
    current: gitops.k8s.aws/v1beta1
    target: gitops.k8s.aws/v1beta1
    status: no-source
    duplicate_group: eks-cluster-composite-api-version

  - component: SealedSecret group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/secrets/git-secret.yaml
    locator: apiVersion (line 1)
    current: bitnami.com/v1alpha1
    target: bitnami.com/v1alpha1
    status: upgrade
    source_url: https://github.com/bitnami-labs/sealed-secrets/releases
    consulted: 2026-11-20
    duplicate_group: sealed-secret-group-version
    notes: >-
      Target equals current — bitnami.com/v1alpha1 is still the SealedSecret
      group-version at chart 2.20.0. Confirm against the CRDs the target
      chart ships. FINDING 15: `upgrade` is the residual flag and is correct
      for an already-current entry.

  - component: SealedSecret group-version
    category: k8s-api-version
    file: repos/gitops-system/workloads/template/git-secret.yaml
    locator: apiVersion (line 1)
    current: bitnami.com/v1alpha1
    target: bitnami.com/v1alpha1
    status: upgrade
    source_url: https://github.com/bitnami-labs/sealed-secrets/releases
    consulted: 2026-11-20
    duplicate_group: sealed-secret-group-version

  - component: SealedSecret group-version
    category: k8s-api-version
    file: repos/gitops-workloads/template/app-template/git-secret.yaml
    locator: apiVersion (line 1)
    current: bitnami.com/v1alpha1
    target: bitnami.com/v1alpha1
    status: upgrade
    source_url: https://github.com/bitnami-labs/sealed-secrets/releases
    consulted: 2026-11-20
    duplicate_group: sealed-secret-group-version

  - component: aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-aws-provider-config.yaml
    locator: apiVersion (line 17)
    current: aws.crossplane.io/v1beta1
    target: aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-aws-providerconfig-group-version
    notes: >-
      Retained group-version — design area C keeps the provider-aws
      monolith, so the group and version are unchanged subject to per-group
      confirmation against the CRDs shipped in v0.59.0 (task 4.1,
      Requirement 4 criteria 5 and 6). FINDING 15 settles the status flag:
      `upgrade` is the residual flag, so it is correct for a retained
      group-version whose target equals its current value.

  - component: aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-aws-provider-config/aws-providerconfig.yaml
    locator: apiVersion (line 2)
    current: aws.crossplane.io/v1beta1
    target: aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-aws-providerconfig-group-version

  - component: dynamodb.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/apps-manifests/product-catalog-api-manifests/v2-staging/kubernetes/overlays/staging/infra.yaml
    locator: apiVersion (line 1)
    current: dynamodb.aws.crossplane.io/v1alpha1
    target: dynamodb.aws.crossplane.io/v1alpha1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    notes: >-
      Task 4.1 (FINDING 23): confirmed served by provider-aws v0.59.0.
      package/crds/dynamodb.aws.crossplane.io_tables.yaml @ v0.59.0 —
      Table v1alpha1 is served: true, storage: true. Sample app table.

  - component: ec2.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 48)
    current: ec2.aws.crossplane.io/v1beta1
    target: ec2.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-ec2-group-version

  - component: ec2.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 69)
    current: ec2.aws.crossplane.io/v1beta1
    target: ec2.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-ec2-group-version

  - component: ec2.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 96)
    current: ec2.aws.crossplane.io/v1beta1
    target: ec2.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-ec2-group-version

  - component: ec2.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 134)
    current: ec2.aws.crossplane.io/v1beta1
    target: ec2.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-ec2-group-version

  - component: ec2.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 172)
    current: ec2.aws.crossplane.io/v1beta1
    target: ec2.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-ec2-group-version

  - component: ec2.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 210)
    current: ec2.aws.crossplane.io/v1beta1
    target: ec2.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-ec2-group-version

  - component: ec2.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 248)
    current: ec2.aws.crossplane.io/v1beta1
    target: ec2.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-ec2-group-version

  - component: ec2.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 264)
    current: ec2.aws.crossplane.io/v1beta1
    target: ec2.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-ec2-group-version

  - component: ec2.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 280)
    current: ec2.aws.crossplane.io/v1beta1
    target: ec2.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-ec2-group-version

  - component: ec2.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 320)
    current: ec2.aws.crossplane.io/v1beta1
    target: ec2.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-ec2-group-version

  - component: ec2.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 360)
    current: ec2.aws.crossplane.io/v1beta1
    target: ec2.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-ec2-group-version

  - component: ec2.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 405)
    current: ec2.aws.crossplane.io/v1beta1
    target: ec2.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-ec2-group-version

  - component: ec2.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 443)
    current: ec2.aws.crossplane.io/v1beta1
    target: ec2.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-ec2-group-version

  - component: eks.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 481)
    current: eks.aws.crossplane.io/v1beta1
    target: eks.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20

  - component: eks.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 544)
    current: eks.aws.crossplane.io/v1alpha1
    target: eks.aws.crossplane.io/v1alpha1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    notes: >-
      Task 4.1 (FINDING 23): confirmed served by provider-aws v0.59.0.
      package/crds/eks.aws.crossplane.io_nodegroups.yaml @ v0.59.0 —
      NodeGroup v1alpha1 is served: true, storage: true.

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/crossplane-iam.yaml
    locator: apiVersion (line 17)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/crossplane-iam.yaml
    locator: apiVersion (line 20)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/crossplane-iam.yaml
    locator: spec.patches[].target.group + .version (line 27)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/crossplane-iam.yaml
    locator: spec.patches[].target.group + .version (line 36)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/crossplane-iam.yaml
    locator: spec.patches[].target.group + .version (line 51)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/eks-cluster.yaml
    locator: apiVersion (line 20)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/external-secrets-iam.yaml
    locator: apiVersion (line 17)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/external-secrets-iam.yaml
    locator: apiVersion (line 20)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/external-secrets-iam.yaml
    locator: apiVersion (line 23)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/external-secrets-iam.yaml
    locator: spec.patches[].target.group + .version (line 30)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/external-secrets-iam.yaml
    locator: spec.patches[].target.group + .version (line 39)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters-config/template/external-secrets-iam.yaml
    locator: spec.patches[].target.group + .version (line 51)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/aws-ebs-csi-iam.yaml
    locator: spec.patches[].target.group + .version (line 28)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/aws-ebs-csi-iam.yaml
    locator: spec.patches[].target.group + .version (line 38)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/aws-load-balancer-controller-iam.yaml
    locator: spec.patches[].target.group + .version (line 28)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/aws-load-balancer-controller-iam.yaml
    locator: spec.patches[].target.group + .version (line 38)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/aws-load-balancer-controller-iam.yaml
    locator: spec.patches[].target.group + .version (line 51)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/karpenter-config.yaml
    locator: spec.patches[].target.group + .version (line 39)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/karpenter-config.yaml
    locator: spec.patches[].target.group + .version (line 48)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/karpenter-config.yaml
    locator: spec.patches[].target.group + .version (line 60)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/karpenter-config.yaml
    locator: spec.patches[].target.group + .version (line 72)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/karpenter-config.yaml
    locator: spec.patches[].target.group + .version (line 84)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/karpenter-iam.yaml
    locator: spec.patches[].target.group + .version (line 23)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/karpenter-iam.yaml
    locator: spec.patches[].target.group + .version (line 32)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/karpenter-iam.yaml
    locator: spec.patches[].target.group + .version (line 47)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/aws-ebs-csi-iam/role.yaml
    locator: apiVersion (line 1)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/aws-ebs-csi-iam/role.yaml
    locator: apiVersion (line 28)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/aws-load-balancer-controller-iam/policy.yaml
    locator: apiVersion (line 1)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/aws-load-balancer-controller-iam/role.yaml
    locator: apiVersion (line 1)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/aws-load-balancer-controller-iam/role.yaml
    locator: apiVersion (line 28)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 581)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/roles.yaml
    locator: apiVersion (line 2)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/roles.yaml
    locator: apiVersion (line 26)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/roles.yaml
    locator: apiVersion (line 38)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/roles.yaml
    locator: apiVersion (line 62)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/roles.yaml
    locator: apiVersion (line 74)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/roles.yaml
    locator: apiVersion (line 86)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/roles.yaml
    locator: apiVersion (line 98)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-iam/crossplane-iam.yaml
    locator: apiVersion (line 2)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-iam/crossplane-iam.yaml
    locator: apiVersion (line 32)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-iam/crossplane-iam.yaml
    locator: apiVersion (line 45)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/external-secrets-iam/external-secrets-iam.yaml
    locator: apiVersion (line 2)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/external-secrets-iam/external-secrets-iam.yaml
    locator: apiVersion (line 32)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/external-secrets-iam/external-secrets-iam.yaml
    locator: apiVersion (line 60)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/karpenter-config/role.yaml
    locator: apiVersion (line 1)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/karpenter-config/role.yaml
    locator: apiVersion (line 25)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/karpenter-config/role.yaml
    locator: apiVersion (line 35)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/karpenter-config/role.yaml
    locator: apiVersion (line 45)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/karpenter-config/role.yaml
    locator: apiVersion (line 55)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/karpenter-iam/policy.yaml
    locator: apiVersion (line 1)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/karpenter-iam/role.yaml
    locator: apiVersion (line 1)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/karpenter-iam/role.yaml
    locator: apiVersion (line 28)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-workloads/template/app-template/app-iam.yaml
    locator: apiVersion (line 2)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-workloads/template/app-template/app-iam.yaml
    locator: apiVersion (line 32)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: iam.aws.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-workloads/template/app-template/app-iam.yaml
    locator: apiVersion (line 47)
    current: iam.aws.crossplane.io/v1beta1
    target: iam.aws.crossplane.io/v1beta1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-aws/releases/tag/v0.59.0
    consulted: 2026-11-20
    duplicate_group: crossplane-iam-group-version

  - component: apiextensions.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/compositeresourcedefinition.yaml
    locator: apiVersion (line 2)
    current: apiextensions.crossplane.io/v1
    target: apiextensions.crossplane.io/v1
    status: upgrade
    source_url: https://github.com/crossplane/crossplane/releases/tag/v1.20.12
    consulted: 2026-11-20
    duplicate_group: crossplane-apiextensions-group-version
    notes: >-
      Retained group-version served by the Crossplane v1.20 line.
      pkg.crossplane.io/v1alpha1 (ControllerConfig) is deprecated upstream
      but still served; replacing it with DeploymentRuntimeConfig is a
      documented non-goal.

  - component: apiextensions.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 2)
    current: apiextensions.crossplane.io/v1
    target: apiextensions.crossplane.io/v1
    status: upgrade
    source_url: https://github.com/crossplane/crossplane/releases/tag/v1.20.12
    consulted: 2026-11-20
    duplicate_group: crossplane-apiextensions-group-version

  - component: pkg.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/clusters/template/crossplane.yaml
    locator: spec.patches[].target.group + .version (line 28)
    current: pkg.crossplane.io/v1alpha1
    target: pkg.crossplane.io/v1alpha1
    status: upgrade
    source_url: https://github.com/crossplane/crossplane/releases/tag/v1.20.12
    consulted: 2026-11-20
    duplicate_group: crossplane-controllerconfig-group-version
    notes: >-
      FINDING 7. Nested two levels deep: a patch body that adds a patches
      array whose own target names pkg.crossplane.io/v1alpha1. Retained —
      ControllerConfig is deprecated upstream but still served by the
      Crossplane v1.20 line, and replacing it with DeploymentRuntimeConfig
      is a documented non-goal.

  - component: pkg.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-aws-provider/aws-provider.yaml
    locator: apiVersion (line 2)
    current: pkg.crossplane.io/v1alpha1
    target: pkg.crossplane.io/v1alpha1
    status: upgrade
    source_url: https://github.com/crossplane/crossplane/releases/tag/v1.20.12
    consulted: 2026-11-20
    duplicate_group: crossplane-controllerconfig-group-version

  - component: pkg.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-aws-provider/aws-provider.yaml
    locator: apiVersion (line 16)
    current: pkg.crossplane.io/v1
    target: pkg.crossplane.io/v1
    status: upgrade
    source_url: https://github.com/crossplane/crossplane/releases/tag/v1.20.12
    consulted: 2026-11-20
    duplicate_group: crossplane-provider-group-version

  - component: pkg.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-k8s-provider/k8s-provider.yaml
    locator: apiVersion (line 2)
    current: pkg.crossplane.io/v1alpha1
    target: pkg.crossplane.io/v1alpha1
    status: upgrade
    source_url: https://github.com/crossplane/crossplane/releases/tag/v1.20.12
    consulted: 2026-11-20
    duplicate_group: crossplane-controllerconfig-group-version

  - component: pkg.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-k8s-provider/k8s-provider.yaml
    locator: apiVersion (line 12)
    current: pkg.crossplane.io/v1
    target: pkg.crossplane.io/v1
    status: upgrade
    source_url: https://github.com/crossplane/crossplane/releases/tag/v1.20.12
    consulted: 2026-11-20
    duplicate_group: crossplane-provider-group-version

  - component: kubernetes.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 605)
    current: kubernetes.crossplane.io/v1alpha1
    target: kubernetes.crossplane.io/v1alpha1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-kubernetes/releases/tag/v1.3.1
    consulted: 2026-11-20
    duplicate_group: crossplane-k8s-object-group-version
    notes: >-
      Task 4.1 (FINDING 23): confirmed served by provider-kubernetes v1.3.1.
      package/crds/kubernetes.crossplane.io_objects.yaml @ v1.3.1 — Object
      v1alpha1 is served: true, but storage: false and deprecated: true (v1.3.1
      adds a v1alpha2 Object as the storage version). Served satisfies
      Requirement 4.5; migrating the Object bases to v1alpha2 is a non-breaking
      future cleanup, out of scope here. ProviderConfig v1alpha1 (used at
      k8s-provider-config, lines below) is served: true, storage: true.

  - component: kubernetes.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 650)
    current: kubernetes.crossplane.io/v1alpha1
    target: kubernetes.crossplane.io/v1alpha1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-kubernetes/releases/tag/v1.3.1
    consulted: 2026-11-20
    duplicate_group: crossplane-k8s-object-group-version

  - component: kubernetes.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 683)
    current: kubernetes.crossplane.io/v1alpha1
    target: kubernetes.crossplane.io/v1alpha1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-kubernetes/releases/tag/v1.3.1
    consulted: 2026-11-20
    duplicate_group: crossplane-k8s-object-group-version

  - component: kubernetes.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/composition.yaml
    locator: apiVersion (line 714)
    current: kubernetes.crossplane.io/v1alpha1
    target: kubernetes.crossplane.io/v1alpha1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-kubernetes/releases/tag/v1.3.1
    consulted: 2026-11-20
    duplicate_group: crossplane-k8s-object-group-version

  - component: kubernetes.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-k8s-provider-config.yaml
    locator: apiVersion (line 17)
    current: kubernetes.crossplane.io/v1alpha1
    target: kubernetes.crossplane.io/v1alpha1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-kubernetes/releases/tag/v1.3.1
    consulted: 2026-11-20
    duplicate_group: crossplane-k8s-object-group-version

  - component: kubernetes.crossplane.io group-version
    category: k8s-api-version
    file: repos/gitops-system/tools/crossplane/crossplane-k8s-provider-config/k8s-providerconfig.yaml
    locator: apiVersion (line 102)
    current: kubernetes.crossplane.io/v1alpha1
    target: kubernetes.crossplane.io/v1alpha1
    status: upgrade
    source_url: https://github.com/crossplane-contrib/provider-kubernetes/releases/tag/v1.3.1
    consulted: 2026-11-20
    duplicate_group: crossplane-k8s-object-group-version

  - component: EKSCluster composite API version
    category: k8s-api-version
    file: repos/gitops-system/tools-config/crossplane-eks-composition/compositeresourcedefinition.yaml
    locator: spec.group + spec.versions[0].name (lines 7 and 15-16)
    current: gitops.k8s.aws/v1beta1
    target: gitops.k8s.aws/v1beta1
    status: no-source
    duplicate_group: eks-cluster-composite-api-version
    notes: >-
      The declaration that serves the composite group-version the two
      apiVersion occurrences consume. Defined by this repository, so no
      source of record (Requirement 1 criterion 8).

